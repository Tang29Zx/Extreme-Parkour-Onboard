# MEMORY.md

## 项目上下文

- 本地路径：`/home/tang/Extreme-Parkour-Onboard`。
- 当前仓库：`Tang29Zx/Extreme-Parkour-Onboard`；原始上游为
  `change-every/Extreme-Parkour-Onboard`，迁移起点为 `master` 提交
  `6f136271c36e3c9ba8c8f9e4e4205f4e16e2d24c`。
- 目标硬件：Unitree Go2、Jetson Orin NX、Intel RealSense D435i。
- 可复用的已验收实现来自 `/home/tang/parkour-go2-deploy`。

## 长期约束

- D435i推理输入固定为`424×240` Z16、30 FPS，不旋转、不镜像、不翻转。
- 必须读取设备`depth_scale`转米；`0`、`65535`、非正值和超过2米的深度按2米处理。
- 先做`4×4`面积降采样得到`60×106`，再执行`[:-2, 4:-4]`和bicubic缩放得到
  `58×87`，最后按`0～2 m`归一化；bicubic之后不增加截断。
- Isaac内部资产顺序是`FL/FR/RL/RR`，但训练环境在actor输入和动作边界使用
  `[3,4,5,0,1,2,9,10,11,6,7,8]`重排。因此`model_38300`的actor契约实际是
  `FR/FL/RR/RL`，与Unitree电机和脚端力一致；LowState、LowCmd和足端力都必须
  identity映射。导出包装器没有内嵌重排。
- 2026-07-27全链路复核推翻了早先“模型顺序是`FL/FR/RL/RR`”的判断。二次重排
  会交换前后腿的左右动作与动态观测；对称默认站姿和映射往返测试无法发现该错误。
  重新同步Jetson并通过dry-run前禁止再次真机接管。
- 不存储凭据，不自动运行真实电机入口。

## 已完成迁移（2026-07-27）

- 新增纯函数`depth_processing.py`，并将视觉节点切换到已验收的
  `424×240 -> 60×106 -> 58×87`、0～2米训练等价路径。
- 视觉节点已删除手工crop、3米归一化、adaptive average pooling、RealSense
  滤波器和无关的`ros2_numpy`/CRC依赖；预览话题现在发布米制`32FC1`网络输入。
- D435i采集会拒绝超时、缺帧、帧号未前进或相机时间戳未前进的数据。
- 8个聚焦测试、Python 3.8语法检查和`git diff --check`通过；尚未在该仓库中
  重新连接D435i或运行ROS节点。

## Dry-run输出边界（2026-07-27）

- dry-run的LowCmd只发布到随机`/lowcmd_dryrun_<id>`，不使用真实`/lowcmd`。
- dry-run不创建Sport Mode和robot-state API发布端，函数边界也会拒绝请求。
- `--nodryrun`保持上游真实话题、模型和策略数学，接管时必须通过真实输出保护。
- 3个输出路由测试通过；未连接ROS图或真实机器人执行控制节点。

## Jetson环境（2026-07-27）

- 现有CUDA虚拟环境位于`~/parkour/parkour_venv`，Unitree ROS消息工作空间位于
  `~/unitree_msgs_ws`，CycloneDDS工作空间位于`~/cyclonedds_ws`。
- 上游`rsl_rl`包含未使用的torchvision导入，控制入口包含未使用的cv2导入；
  已删除两个假依赖，不为此改动Jetson的NVIDIA Torch。
- 使用`scripts/setup_jetson.sh`做一次性内置包安装和环境验收；新终端使用
  `source scripts/jetson_env.sh`。
- Foxy的`setup.bash`会读取可选的未定义变量，因此包装脚本不能在source ROS环境时
  开启Bash `nounset` (`set -u`)。

## 真实接管保护（2026-07-27）

- 真实模式在模型warm-up和L1连续1秒确认前不创建`/lowcmd` publisher；L1确认后、
  ReleaseMode前预创建publisher和消息缓冲，但此时真实发布仍被硬门槛禁止。
- MotionSwitcher使用CheckMode/ReleaseMode/CheckMode确认链；模式已为空时跳过
  ReleaseMode。
- ReleaseMode前锁存新鲜、近似静止的实测关节位姿；最终CheckMode确认为空后
  立即发布该位姿保持命令。原生publisher endpoint的DDS销毁延迟不再阻塞接管，
  消除了已观测到的约5.75秒无有效低层保持命令空窗。
- 接管后从锁存关节位姿用3秒五次平滑过渡到策略默认姿态，再保持0.5秒重建至少
  10帧本体历史和5帧视觉GRU状态；LowState或深度超过0.25秒会重新预热，遥控器
  只在Y请求当帧检查。L2恢复后走同一流程。
- Y后的第一帧严格保持站姿，随后用1秒五次渐变和0.05 rad/周期上限追赶策略目标；
  稳态继续执行0.10 rad/周期、机械限位和PD力矩约束。`last_actions`始终保存上一帧
  actor原始输出，与训练端在延迟和裁剪前写动作历史的语义一致。
- policy每周期检查LowState、深度、关节位置和速度；深度或输出保护失败走站姿恢复，
  LowState超过0.25秒则发送motor-off尾帧并进入硬停止。
- L2只用1秒退回站姿，不在`/lowcmd` publisher存在时恢复Sport Mode；R2和
  进程退出发送10帧motor-off。
- 预创建publisher后，最终CheckMode为空前，普通腿命令和退出motor-off都不得发布。
- 控制节点不再逐帧打印yaw、动作tensor和分段耗时；最近250个控制周期及50次视觉
  更新保存在内存，L2恢复完成、R2关电机后、异常或退出时写入时间戳NPZ。
- 32个测试、Python 3.8语法检查和diff检查通过；新policy prime、Y接入保护、飞行
  记录和Jetson循环p95尚未在Jetson dry-run或真机验收。

## 权重来源

- 2026-07-28 15:03真机记录分析后，用户要求下一轮改测`model_38300`；本地
  `traced/`已经替换为训练仓库
  `legged_gym/logs/box_parkour/exports/model_38300`的完整配套包，
  `base_jit.pt`、`vision_weight.pt`和`config.json`必须保持同一版本。
- 三个文件的SHA-256依次为
  `b556db21f30f17e83fcd240b6f0d534b72f255aec42109cb3c223ac8ad756fb4`、
  `897758d6e775419993973fc6f75c096631267b51332d6ed0d1bbb576d9551793`和
  `4056bb5cf389a9827d57d23afc9c4a57aeb3a44c30a62c5ac8500de3ce3a77d7`。
- 替换前的`model_38700`完整包保存在本机
  `/home/tang/.cache/extreme-traced-38700-backup-Gtq41O/`；该备份未同步Jetson。
- 目标加载链已验证视觉输出`(1, 34)`、拼接观测`(1, 114)`、动作`(1, 12)`，
  且所有输出有限；`model_38300`尚未同步Jetson或重新做真机测试。
- 上游示例权重仍可从Git恢复，不单独保存副本。

## 2026-07-27真机故障诊断

- `~/extreme-control.log`记录约5秒策略运行：216帧动作全部有限，L2在1秒内正常退出，
  没有Python异常、NaN或控制报错。但该版本按错误的`FL/FR/RL/RR`actor假设记录
  和下发数据，因此日志不能证明左右腿映射正确。
- 218个计时样本中p50约15.63 ms、p95约50.89 ms，14帧超过20 ms；首次warm-up
  样本不属于稳定控制周期，运行期仍存在少量超期，需要后续降低逐帧终端输出。
- 停止控制后`/lowcmd` publisher为0。连续1500帧LowState显示RR/RL hip温度约
  `69/68°C`，随后降至`67/66°C`，其余腿约`37～42°C`；RL thigh和RL calf的
  `lost`持续为5且未增长，其余电机为0。
- BMS约31.45 V、95% SOC，电芯约4098～4102 mV，未见明显电池异常。
- 当前证据支持后髋高温或RL历史通信丢失与故障灯相关，但Unitree公开消息文档未
  定义`lost`值语义或温度告警阈值，不能仅凭该字段确定最终故障码。冷却、正常
  重启并采集无自定义控制的基线前，禁止再次真机接管。
- 后续日志确认另一次故障发生在策略启动前：Sport Mode于17:28:46释放，自有
  `/lowcmd`直到17:28:52才创建，约5.75秒空窗内后腿先塌下；随后从塌下位姿恢复
  站姿触发红灯。原生Sport Mode正常且该次没有策略动作，根因转向低层接管时序，
  因此删除“等待原生publisher endpoint消失”门槛，改为预创建但确认释放前禁发。
- 修复接管空窗后的支架测试中，释放确认到锁存位姿首帧仅约5.6 ms且3秒站姿正常；
  用户按Y后发生侧翻，但`~/extreme-control-handoff.log`在`Startup ramp complete`
  后停止，没有记录Y或动作，无法复原该次精确LowCmd。Jetson代码和三个模型资产
  哈希与本地一致，事后LowState约为roll -2.9°、pitch -0.1°、12电机`lost=0`。
- 上一次完整策略日志显示同一模型第一帧相对站姿最大目标跳变约0.453 rad，且左右
  明显不对称；同时warm-up到Y之间的10帧本体历史和视觉GRU状态长期未刷新。因此
  后续真机前必须先通过状态重建、Y短时接入保护和飞行记录的dry-run验收。

## model_38300动作裁剪契约（2026-07-27）

- 训练源码将`normalization.clip_actions=1.2`解释为缩放后的关节残差上限，并用
  `clip_actions / action_scale`计算策略空间阈值；`action_scale=0.25`时策略动作裁到
  `±4.8`，最大关节残差为`±1.2 rad`。
- 训练端PD目标和参考部署均使用
  `target_q = default_q + clipped_action * action_scale`；保护后目标反算动作使用
  `(commanded_q - default_q) / action_scale`。
- 训练环境在动作延迟与裁剪前更新`action_history_buf`，观测时重排回actor顺序；
  因此`self.actions`和`last_actions`必须保存actor原始输出。裁剪后请求和保护后执行
  动作只用于目标计算与飞行记录。

## 导出策略单箱回放坐标（2026-07-27）

- `legged_gym/scripts/replay_traced.py`的CPU深度射线必须基于每个环境实际选择的
  `env_origins`计算箱体世界坐标，不能将箱体固定在第一行赛道的世界坐标。
- 修复前固定seed会让机器人出生在第二行：真实碰撞箱约在`x=20～21.2 m`，视觉箱
  仍在`x=2～3.2 m`，策略因此在`x=19.89 m`卡住。修复后同一1000步回放在第250步
  已到`x=22.16 m`并越过单箱，坐标纯函数测试通过。

## Y后向左侧翻记录与修复（2026-07-27）

- 飞行记录`extreme-flight-20260727-194701-319410.npz`曾被分析为包含Y后43帧接入和
  51帧L2恢复。第一帧目标无跳变、最大单周期变化0.05 rad、动作有限、控制p95约
  8.74 ms、深度年龄最大约40 ms；这些事实排除了明显时序和非有限输出，但记录字段
  按错误映射标记，不能排除左右腿二次重排。
- Y前roll/pitch约`-2.29°/-1.40°`，Unitree原始脚端力按`FR/FL/RR/RL`为
  `[32,22,29,11]`。旧阈值25会把FL和RL写成离地；训练端使用低阈值并将当前接触与
  上一帧逻辑或。该接触错误和关节二次重排同时存在，不能只凭侧翻方向区分贡献。
- 真机接触阈值改为5并增加训练一致的一帧接触记忆。该值来自本次日志承重脚最低
  8～11、离地脚为0的样本，不是Unitree官方标定值，部署前需在目标地面复核。
- policy prime和Y瞬间新增四足承重、8°姿态、0.2 rad关节跟踪门槛；高温按用户
  要求只记录、不阻断。`lost`是累计计数：prime以当前值为基线，只在继续增长时
  重启，不能要求绝对值为0。接入前1秒增加相对起点±0.3 rad包络并使用0.05 rad
  周期限速；稳态使用0.10 rad并持续执行机械限位和PD力矩约束。
- 用户明确不要接入期或L2基于倾斜角自动motor-off：接入后的姿态、支撑和温度只
  记录，R2仍是人工motor-off急停，L2保持原有的一秒站姿渐变。
- 飞行记录升级为v2，新增接触判定、12电机温度、`lost`和估算力矩。19个聚焦测试
  在Python 3.8容器中通过，相关运行文件语法检查和diff检查通过；修改尚未同步Jetson
  或做硬件验收。
- 20:08后的L1站姿连续观测为roll约`-2.42°`、pitch约`-1.60°`，四足力
  `[32,26,30,12]`，最大关节跟踪误差约0.145 rad。RR calf的`lost=5`在496帧内
  恒定且电机正常跟踪，证明绝对`lost=0`门槛会误阻Y。遥控器空闲时不持续发布，
  因此prime只检查LowState/深度，Y事件当帧单独检查遥控器新鲜度。

## sim2real全链路复核与修复（2026-07-27）

- 权威训练源码位于`/home/tang/extreme-parkour-go2`。Isaac DOF顺序是
  `FL/FR/RL/RR`，`step()`先把actor动作重排到Isaac顺序，观测又把关节、动作历史和
  接触重排回`FR/FL/RR/RL`；`save_jit.py`没有在导出模型中加入重排。Onboard原始
  提交`6f13627`的identity映射与该契约一致。
- 修复当前控制端的关节、默认角、动作和足端identity映射。真实验收D435记录的一帧
  视觉输出经`base_jit.pt`前向后，旧二次重排与修复后物理电机目标最大相差
  0.343127 rad，L2相差0.587423 rad，说明该错误足以造成明显左右不对称控制。
- `last_actions`恢复为上一帧actor原始输出。policy每周期增加LowState/深度新鲜度、
  关节位置和URDF速度检查；速度检查保留0.5%相对测量容差。LowState超时硬停止，
  其他保护失败进入站姿恢复。接入首1秒使用0.05 rad/周期，之后以0.10 rad/周期与
  机械限位及PD力矩可行区间求交；交集为空时也必须硬停止。
- 全套43个单元测试通过，相关文件通过Python 3.8语法检查和`git diff --check`；
  导出actor离线前向全部有限。没有运行Jetson dry-run、ROS图或真实机器人。
- 仓库内置`legged_gym`是较旧训练副本，未完整包含生成`model_38300`的动作延迟、
  观测噪声和批量深度实现。它可用于当前回放，但重新训练前必须以权威训练仓库同步
  并重新验证，不能假定内置源码可复现现有权重。
- MEMORY中提到的历史飞行文件当前不在本机仓库；除已有摘要外无法重新核验原始列。
  真机前仍需关注后髋66～69°C和历史`lost`计数，并在承重支架上完成新版本验收。

## Unitree边界S2S（2026-07-28）

- 新增消息无关的LowState/LowCmd边界，生产ROS节点和Isaac回放共用同一套关节、足端、
  IMU、53维proprio和LowCmd转换；Isaac原始顺序与Unitree顺序不再由两个入口重复实现。
- 边界回放默认提供蓝色直连、绿色正确边界、红色旧二次重排三条赛道；红色故障只在
  回放脚本存在。每帧显式验证LowCmd目标经环境内部重排后与PhysX PD目标逐维一致。
- B停机由三个问题叠加造成：0.05 rad被错误地永久用作稳态限速；渐变器按候选目标推进
  而未接收后级实际下发目标反馈；URDF速度上限被当成无浮点/测量容差的硬断言。现改为
  首1秒0.05 rad、稳态0.10 rad，渐变器每周期提交实际下发目标，并使用0.5%速度容差。
- Go2关节和力矩限值集中到边界模块，力矩使用URDF的`23.7/23.7/35.55 Nm`。回放
  关闭旧环境0～0.9 rad reset噪声，箱体通过阈值按真实后缘加0.2米裕量计算为
  `x=3.40 m`，到达目标后的成功终止不再计为意外reset。
- Python 3.8 CPU PhysX的`fixed/full`和`abc/full`都在第426步由绿色B越过箱体后缘；
  单路最大接入/稳态目标变化0.05/0.10 rad、最大估算力矩比例0.8535，无reset、fault、
  机械限位越界或LowCmd到PhysX目标差异。红色C因旧二次重排导致入口跟踪误差超过
  0.2 rad而被拒绝，因此保持站姿，这是预期故障对照而非脚本卡死。
- S2S不验证DDS、CRC、固件、电机通信或真实脚端力尺度；`full`通过只解除映射和控制
  数学阻塞，不构成真机策略接管验收。
- 当前RTX 5070 Ti、580.159.03驱动主机的非headless Isaac Gym在Viewer初始化阶段
  退出139，虽然X Display可访问且CPU headless正常；三色Viewer尚未实际渲染验收，
  需要使用已知兼容Isaac Gym的GPU/驱动环境。
- 修复后全套51个Python 3.8单元测试、相关语法检查和`git diff --check`通过；
  `fixed/full --headless`和`abc/full`验收均为PASS。没有启动ROS、DDS、Jetson
  dry-run或真机输出。离线三路回放保存在
  `~/extreme-boundary-s2s/replays/unitree-boundary-abc-repaired.mp4`。
- 追加模拟遥控和粗糙地面闭环：生产节点与回放共用`RemoteEdgeTracker`，回放显式经过
  L1、3秒站姿、0.5秒prime和Y，L1前的LowCmd授权保持false。种子17、±5 mm、
  0.2 m噪声块的绿色完整保护回放在第422步越过`x=3.4076 m`，0 reset、0 fault，
  最大接入/稳态目标变化0.05/0.10 rad，最大估算力矩比例0.8812。NPZ位于
  `~/extreme-boundary-s2s/remote-noise/unitree-boundary-s2s-remote-noise-pass.npz`；
  全套53个Python 3.8单元测试、语法和diff检查通过。

## stand_hold lost基线修复（2026-07-28）

- 真机日志显示policy prime完成约418秒后第一次Y被“motor lost counters increased”
  拒绝，随后重新prime立即通过；同时只读LowState观测到一个历史`lost=5`且短时未增长。
- 根因是prime期间逐周期更新`lost`基线，但进入`stand_hold`后基线冻结，等待期间任意一次
  累计增长都会被延迟到下一次Y才发现，不能代表Y当下仍在丢包。
- `stand_hold`现在以50 Hz持续推进基线；增长时立即自动重新执行完整0.5秒prime，
  稳定后旧增长不再拒绝Y。prime期间和Y当帧的新增长仍会拒绝，安全门槛没有放宽。
- 54个Python 3.8单元测试、相关语法检查和`git diff --check`通过；修改尚未同步Jetson，
  也未在新版本dry-run或真机上验证自动重新prime日志。

## D435i物理方向校准（2026-07-28）

- `traced/config.json`的训练随机范围为相对机身roll/yaw各`±2°`、向下pitch
  `20°～25°`，水平FOV为`86°～90°`。
- 调整前30帧原始`424×240`深度的多区域地面拟合显示相机相对机身仅向下约`1.4°`，
  不符合训练范围；地面拟合高度约`0.435 m`且各区域一致。
- 调整后机身IMU为roll约`-3.51°`、pitch约`-0.20°`；五个图像区域测得相机相对
  地面向下`21.25°～21.49°`，扣除机身姿态后相对机身向下约`21.62°`，roll残差约
  `+0.43°`，水平FOV约`89.47°`，均在训练范围内。地面拟合95%残差为
  `3.2～6.5 mm`，方向校准通过。
- 平地平面不能观测相机相对机身yaw；yaw仍需使用与机身纵轴垂直且居中的竖直标志物
  单独验收。

## 蹬箱动态输出约束尝试与回退（2026-07-28）

- 12:03完整真机记录显示稳态0.10 rad/周期限速在196帧中触发178帧，随后限速区间与
  PD力矩区间失去交集并触发`policy_target_infeasible`；这不是电机速度超限，最大关节
  速度仅为标称值的46%。
- 曾尝试仅在接入首1秒保留0.05 rad/周期与±0.3 rad包络，稳态取消0.10 rad限制并让
  力矩区间优先于步长区间。离线重放与Docker平地/粗糙地面虽通过，但稳态单周期目标
  变化达到0.5137/0.5886 rad，未覆盖真机50 Hz目标到电机固件PD执行的动态差异。
- 用户随后在真机观察到侧翻，因此该尝试不能作为安全修复。现已回退：接入首1秒继续
  使用0.05 rad/周期，稳态恢复0.10 rad/周期，并重新以步长、机械限位和PD力矩区间的
  完整交集为硬条件；交集为空继续触发`policy_target_infeasible`和motor-off。
- 补充的13:22实时终端记录确认Y于13:22:21.198进入policy，接入在13:22:22.207完成；
  稳态采样中的请求/命令差随后为0。13:22:24.017运行期保护以`measured joint position
  exceeded its limit`退出policy并进入站姿恢复，此时此前的2 Hz快照已出现仅FR/RL对角
  支撑、最大跟踪误差0.427 rad；恢复后姿态继续发展到roll约+26°、pitch约-18°，用户
  于13:22:25.096按R2急停。采样峰值实际/预测力矩仅约76.5%/76.7%，关节速度约34.5%，
  LowState和深度新鲜、`lost`全0，因此没有证据表明力矩或速度硬上限先触发。
- 本次成功保存`extreme-flight-20260728-132225-039238.npz`和随后R2产生的
  `extreme-flight-20260728-132225-305147.npz`；第一份尚待从Jetson拉回，用于确定被拒
  周期前的具体越界关节和50 Hz目标跳变。此前拉回的2 Hz文件在Y前被截断，不能代表完整
  运行过程。
- 力矩边界仍为`23.7/23.7/35.55 Nm`，速度上限和PD增益未改。没有完整高频NPZ前，
  不得再次取消稳态目标步长限制；仿真越箱结果不能替代承重支架和真机动态验收。

## 运行中实时状态读取（2026-07-28）

- 控制节点以最高2 Hz向`/extreme_parkour/runtime_status`发布schema v1 JSON；数据复用
  当前控制样本和最近250周期内存记录，不包含原始深度，不在50 Hz控制循环写磁盘。
- 状态覆盖phase、真实LowCmd授权、策略接入、三类输入年龄、roll/pitch、足端接触、
  12关节实测/命令/速度、跟踪误差、动作约束差、12电机温度/`lost`/估算力矩、实际与
  预测PD力矩比例，以及控制循环last/p50/p95/max。构建或DDS发布异常被观测边界捕获，
  不得改变控制状态机或已下发LowCmd。
- `scripts/read_runtime_status.py`是只读终端入口，默认显示单行摘要，并用transient-local
  QoS读取`/rosout`中`unitree_ros2_real`的实时及缓存事件；`--json`显示完整数组，
  `--logger-name`可覆盖过滤名。它不创建控制、Sport API或robot-state publisher。
  57个Python 3.8单元测试、相关语法检查和`git diff --check`通过；ROS 2 Jazzy隔离
  domain 232已用合成状态和warning联调通过，尚未同步Foxy Jetson或连接真实控制节点。

## 真机首次跨箱动作与日志缺口（2026-07-28）

- `nodryrun-20260728-100746.log`确认模型、输入、MotionSwitcher释放、3秒站姿渐变和
  0.5秒prime均正常；控制台后续时间戳确认Y在`1785204529.4777`进入policy，约
  1.008秒后完成接入渐变，用户现场观察到机器人开始执行跨箱动作。
- 该进程启动早于2 Hz实时状态功能同步，因此没有runtime status；`tee`文件在prime
  完成处停止更新，进程结束时也没有生成当天NPZ。内核日志没有OOM、CUDA、segfault
  或磁盘不足证据，无法从现存资产还原跨箱阶段的关节跟踪、温度、`lost`、力矩和循环
  耗时，不能把这次观察描述为完整量化验收。
- Jetson的实际启动时间为10:36:46，`last`将10:07的视觉/控制SSH会话标记为`crash`，
  证明跨箱后的进程被整机重启终止，而非走L2、R2、异常或Ctrl+C清盘路径。飞行记录只
  保存在内存环形缓冲，当前入口没有`SIGHUP`/`SIGTERM`处理，因此重启时未执行
  `flush_flight_record()`，当天NPZ丢失。`tee`本身不会把已显示的完整行延迟28分钟；
  Y和接入完成行没有出现在该文件，说明它们来自未接入这份`tee`的控制台/进程流。
- 10:43只读检查时Jetson已无控制、视觉或状态读取进程。此前复制到
  `extreme-runtime-logs/controller-20260728-103232.log`的是10:32的另一轮启动记录，
  不能用于否定10:08已经发生的策略接入和跨箱动作。

## 真机跨箱大姿态与硬停记录（2026-07-28 11:00）

- 新实时状态链路首次完整记录真机运行：Y在11:00:16.854进入policy，1.006秒后完成
  接入，11:00:20.961因`policy_target_infeasible`发送motor-off，约4.107秒policy数据
  已保存为`extreme-flight-20260728-110021-171243.npz`。其后11:00:27的R2发生在已
  硬停之后，不是第一次停机原因。
- LowState最大年龄18.65 ms、深度最大37.15 ms、循环p95/max为11.42/13.76 ms，
  均低于20 ms控制周期或0.25秒新鲜度门槛；`lost`全程固定为
  `[0,5,5,0,5,0,0,0,0,0,0,0]`，故障不是计算超时、视觉过期或新增电机丢包。
- 机身在稳态策略中由约roll `-9.8°`继续发展到最后记录的`+48.17°`，pitch达到
  `-34.81°`；这是按用户要求不启用运行期姿态自动motor-off后的行为，最终由目标
  可行性硬保护停机。单凭欧拉角不能断言物理侧翻；与相同策略成功跨20 cm单箱的
  S2S参考相比，前1秒真实roll `-9.4°`与仿真`-8.9°`接近，属于正常抬腿/接入动作，
  但成功参考全程roll仅`-11.85°～+4.94°`、pitch仅`-16.60°～+5.67°`。真实轨迹在
  约2.5秒后离开该参考包络，48°属于明显异常大姿态，但缺少同步视频和真实位姿轨迹，
  不能只凭该峰值区分“已经侧翻”与“仍在受控的大幅倾斜”。
- 最后一帧RR calf跟踪误差0.919 rad，策略请求与实际命令仍差0.502 rad；全段最大
  请求/命令差1.192 rad，raw action在RR calf达到5.892并被策略裁剪。最后一帧
  FR thigh跟踪误差0.520 rad、`tau_est=24.268 Nm`，超过23.7 Nm标称值约2.4%；
  其PD可行上界已经与当前命令重合。RR calf的剩余可行区间也仅0.126 rad。
  被拒绝周期在`send_action()`内抛出后没有进入飞行记录，因此无法逐字确认报错关节；
  最后一帧证据表明FR thigh最可能先失去步长/力矩交集，RR calf是第二风险点。
- 最高温度为RL hip 70°C、RR hip 65°C；接触和姿态显示机器人正在明显倾倒。当前
  控制进程锁存在`emergency_stop`，不会自动恢复策略，下一次运行必须重新启动。

## model_38700真机大姿态与R2记录（2026-07-28 15:03）

- Jetson端`traced/`三个资产SHA-256与`model_38700`配套包一致。15:03:26.339按Y，
  15:03:27.347完成接入，15:03:31.396由用户按R2急停；控制端没有先触发输入过期、
  关节/速度越界或`policy_target_infeasible`。完整250周期、50视觉帧已拉回本机
  `/home/tang/.cache/extreme-runtime-logs/latest-20260728-1503/`。
- NPZ显示5秒policy内roll范围`-16.20°～+29.33°`、pitch范围
  `-44.14°～+12.68°`；末帧pitch约`-44.14°`、仅RL脚接触且关节速度接近0，
  说明R2前已经形成异常的大幅前倾/倚靠状态，不能视为稳定越箱。
- 输出保护在250周期中修改策略目标221周期，其中稳态修改173周期；最大请求/命令
  差`1.4378 rad`，最大下发步长保持`0.1000 rad`。预测PD力矩有21周期基本贴住
  边界；RR thigh实际`tau_est`峰值`26.02 Nm`，为23.7 Nm标称边界的109.8%，
  FL/RL thigh也短时超过标称值。保护使用预测PD力矩约束目标，实际`tau_est`仅记录，
  因此实际读数超过标称值不会单独触发当前硬停。
- 对173个稳态修改周期重建约束交集，与日志命令最大误差仅`1.4e-7 rad`：
  `0.10 rad/周期`步长边界实际主导161周期，预测PD力矩边界主导21周期，机械限位
  主导10周期（同一周期可由不同关节同时命中多类边界）。力矩主导集中在RR/
  RL/FL thigh的`15/4/2`周期，机械限位集中在RR calf 9周期和FR calf 1周期。
  最大差发生在RR calf：策略请求`-0.3000 rad`，实际命令`-1.7378 rad`，该请求还
  超过RR calf机械上限`-0.83776 rad`；步长窗口因更严格而成为当帧实际截断边界。
- 在固定本次实测`q/dq`的非因果离线反事实中，全关节稳态步长改为`0.11 rad`仅将
  修改周期从173降到171，却使命令轨迹相对原记录最大偏移`0.16 rad`；改为
  `0.12 rad`也只降到167周期，轨迹偏移增至`0.28 rad`且预测力矩仍贴住100%。
  因此当前证据不支持为减少裁剪而直接扩大范围；下一轮`model_38300`先保持
  `0.10 rad`基线。若之后必须做支架A/B，`0.11 rad`应作为单次实验上限，不能据
  此离线重放宣称真机安全或有效。
- 最大关节速度仅为标称上限的45.35%，LowState/深度最大年龄17.08/34.39 ms，
  控制循环p95/max为12.46/13.52 ms且无20 ms超期，`lost`全0，视觉输出全部有限；
  速度、输入时效、循环耗时和非有限推理均不是本次首要问题。最高温度RL hip 69°C。
- R2后NPZ已成功落盘；Jetson随后多次重启，最近一次`reset_reason`为`MAINSWRST`且
  无pstore记录。重启发生在R2和日志保存之后，不是本轮policy停止原因。NVIDIA对
  Tegra PMC的解释确认`MAINSWRST`是SoC软件复位（典型例子为系统`reboot`），不同于
  断电/复位键的`SYS_RESET_N`和看门狗reset；两个项目代码均无执行整机`reboot`或
  `shutdown`的路径。现有证据仍不能确定是用户命令还是其他系统服务发起了软件复位。

## model_38300稳态0.12 rad试验配置（2026-07-28）

- 用户在知悉`model_38700`反事实重放结果后明确要求把稳态策略目标步长从
  `0.10 rad/周期`调到`0.12 rad/周期`，用于下一轮`model_38300`测试。接入首1秒
  仍为`0.05 rad/周期`和相对起点`±0.3 rad`包络，机械限位、速度检查、PD力矩交集
  及`23.7/23.7/35.55 Nm`边界均未改变。
- 该修改当前仅在本地仓库，尚未同步Jetson或驱动真机。此前基于`model_38700`
  实测`q/dq`的离线反事实只用于风险筛查，不构成`model_38300`真机安全证明；验证和
  回滚目标是恢复`POLICY_TARGET_MAX_STEP_RAD = 0.10`。
- 本地Python 3.8全套58个单元测试通过；CPU PhysX的`fixed/full`在±5 mm地面、
  0.2 m噪声块、seed 17下第412步越过20 cm单箱，最大稳态步长0.12 rad，0 reset、
  0 fault，目标映射、机械和PD力矩检查通过。NPZ位于
  `/home/tang/.cache/extreme-parkour-go2-docker/boundary_38300_step012/`；这不覆盖
  0.45 m箱体、Jetson、DDS、固件PD或真机动力学。

## model_38300稳态0.15 rad本地复测配置（2026-07-28）

- 用户明确要求在0.12 rad本地验证后，继续把稳态策略目标步长调整为
  `0.15 rad/周期`复测。接入首1秒仍为`0.05 rad/周期`和相对起点`±0.3 rad`包络；
  机械限位、速度检查、PD力矩交集及`23.7/23.7/35.55 Nm`边界均未改变。
- 该配置只用于本地`model_38300`仿真，尚未同步Jetson或驱动真机。0.15 rad比此前
  真机日志反事实筛查过的0.12 rad更激进；本地回放只能检查闭环兼容性，不能证明
  0.45 m箱体、固件PD或真机安全性。回滚目标仍为
  `POLICY_TARGET_MAX_STEP_RAD = 0.10`。
- 本地Python 3.8全套58个单元测试通过；CPU PhysX的`fixed/full`在与0.12 rad相同的
  ±5 mm地面、0.2 m噪声块、seed 17下第407步越过20 cm单箱，0 reset、0 fault。
  0.12 rad对照为第412步；输出保护修改周期从195降到170，最大预测力矩比例从
  0.8841升到0.9421，pitch下界从-20.31°变为-22.26°，最大速度比例从0.9983变为
  0.9991。NPZ位于
  `/home/tang/.cache/extreme-parkour-go2-docker/boundary_38300_step015/`。

## model_38300稳态0.20 rad本地配置（2026-07-28）

- 用户在0.15 rad本地回放后明确要求继续把稳态策略目标步长调整为
  `0.20 rad/周期`。接入首1秒仍为`0.05 rad/周期`和相对起点`±0.3 rad`包络；
  机械限位、速度检查、PD力矩交集及`23.7/23.7/35.55 Nm`边界均未改变。
- 该配置当前仅在本地仓库，尚未同步Jetson或驱动真机。0.15 rad回放已经把最大
  预测力矩比例提高到0.9421，故0.20 rad必须视为更激进的独立实验配置；在完成
  代表性闭环回放前，不得将其描述为通过仿真或适用于真机。回滚目标仍为
  `POLICY_TARGET_MAX_STEP_RAD = 0.10`。

## model_38300 calf稳态0.40 rad本地配置（2026-07-28）

- 15:58真机记录
  `/home/tang/.cache/extreme-runtime-logs/latest-20260728-1558-step020/flight/`
  中250个控制周期的相邻`commanded_q`变化已复核：12个关节最大值均约为
  `0.200000 rad`，没有关节超过`0.200001 rad`。因此该记录对应全关节统一
  `0.20 rad/周期`的未提交部署态，不应直接用14:04仍为0.10的`fd75280`，也不应
  用19:04已含calf 0.40的`d208a57`代替当时基线；飞行NPZ本身没有保存Git hash。
- 同一飞行NPZ只保存50个视觉时刻的处理后深度`min/max/mean`和34维视觉输出，未保存
  原始深度图、相机内参、外参或机身到相机变换。视觉推理间隔中位数约99.91 ms，
  控制端深度年龄均值/最大值约19.41/35.40 ms，没有输入冻结证据；等效平均深度从
  首帧约0.81 m变到末帧约1.78 m，但机器人同时发生大姿态倾倒，不能据此反推出
  相机安装位姿变化或量化相机松动。
- 用户根据0.20 rad真机视频观察和15:58飞行记录，判断首次失败是前腿calf未及时
  收起、碰到箱沿后才进入乱蹬，明确要求只把四个calf的稳态目标步长提高到
  `0.40 rad/周期`。hip和thigh保持`0.20 rad/周期`；接入首1秒仍为全关节
  `0.05 rad/周期`及相对起点`±0.3 rad`包络。
- 该配置当前仅限本地验证，尚未同步Jetson或驱动真机。0.40 rad约为每20 ms
  `22.92°`目标变化；机械限位、速度检查、PD力矩交集和`23.7/23.7/35.55 Nm`
  边界均保持不变。回滚目标仍为`POLICY_TARGET_MAX_STEP_RAD = 0.10`的统一基线。
- 本地Python 3.8全套59个单元测试通过；CPU PhysX的`fixed/full`在±5 mm地面、
  0.2 m噪声块、seed 17下第394步越过20 cm单箱，0 reset、0 fault。四个calf
  稳态最大步进均为0.40 rad，四个thigh均为0.20 rad，hip最大0.1599 rad；最大预测
  力矩比例达到1.00。NPZ位于
  `/home/tang/.cache/extreme-parkour-go2-docker/boundary_38300_hipthigh020_calf040/`；
  该结果不覆盖0.45 m箱体、Jetson、DDS、固件PD或真机动力学。
- 7月28日19:24的`calf040`真机目录含两个飞行文件：`policy_guard_recovery`文件是
  主记录，最后32个视觉样本组成连续约3.10秒的policy段；随后约0.29秒的R2文件只有
  4个控制周期且无视觉样本，不能用于相机对比。15:58与19:24之间D435采集、预处理
  和网络输入参数没有软件代码变化，两个有效视觉段的推理间隔中位数分别为99.91和
  99.98 ms。19:24段的等效平均深度首/末/均值为1.086/1.221/1.071 m，15:58为
  0.810/1.780/1.589 m；这是不同机身轨迹下的画面内容差异，日志没有外参，不能
  解释为相机安装位姿变化。用户随后澄清：对比基线是15:59的`model_38300`运行，
  19:24运行发生在实体D435i倒装之后，并且曾在另一部署仓库实现180°补偿。Jetson
  只读核查进一步确认：补偿确实存在于`parkour-go2-deploy/extreme_go2/visual_node.py`
  中，在降采样/裁剪前使用`raw_depth[::-1, ::-1]`且默认角度为180°；但19:24的实际
  `visual.log`节点名和唯一启动字符串来自`Extreme-Parkour-Onboard/
  visual_extreme_parkour.py`。该实际发布`/forward_depth_image`的文件及其时间戳型
  Python字节码都只执行`np.asanyarray(...).copy()`后直接预处理，不含旋转。因此
  “Jetson上存在并验证过补偿代码”不等于“19:24生产推理链路使用了补偿”；19:23的
  姿态/地面验收也不能证明旧Onboard视觉节点已接入该逻辑。飞行NPZ没有保存原始图、
  相机旋转配置或源码哈希，7月29日相机已恢复并确认为正装。
- 用户观察19:24倒装后机器人尚未到箱体就提前向上扒。按Y后约3.0秒对齐，15:59
  正装记录的FR calf安全约束前`requested_q`约为`-1.364 rad`，19:24倒装记录约为
  `-0.593 rad`；相对默认`-1.5 rad`，后者已主动伸直约0.91 rad。两次均为
  `model_38300`且hip/thigh稳态步长均为0.20，说明提前伸腿信号来自actor及其输入/
  闭环历史，而不是calf 0.40输出限位直接生成。倒装把训练仅覆盖`±2°`roll的深度图
  旋转约180°且软件未补偿，是首要原因；calf从0.20放到0.40会更快执行错误请求，
  属于次要放大因素。19:24策略在19:24:51失败，而D435i首次断连在19:26:40，故后者
  不是本次动作原因；真实起点到箱体距离未记录，仍是剩余混杂变量。
- 19:24的`policy_guard_recovery`不是Jetson或控制进程重启：状态机退出policy、执行
  1秒站姿恢复后进入prime，随后用户按R2。被安全校验拒绝的周期不会写入NPZ，故无
  精确错误字符串；最后安全policy帧FL calf实测`-2.73296 rad`，首个恢复帧为
  `-2.75917 rad`且速度已由负转正，距离带0.05 rad容差的下界`-2.7727 rad`仅
  `0.01353 rad`。深度/LowState新鲜、已记录最大速度低于硬限，最可能是中间被拒绝
  帧的FL calf位置瞬时越过机械下界后回弹；速度瞬时越界是次选，无法从缺失帧排除。

## model_38300随机化深度单箱完整边界评测（2026-07-29）

- 联合评测入口位于权威训练仓库
  `legged_gym/legged_gym/scripts/evaluate_onboard_single_box.py`，运行时从本仓库显式
  加载生产`joint_mapping/policy_context/real_control_safety/unitree_boundary`纯函数，
  不复制安全常量。闭环使用Isaac真实深度相机、生产53维本体观测和历史、视觉GRU、
  LowState/LowCmd、3秒站姿、0.5秒prime、1秒接入及当前hip/thigh 0.20 rad、calf
  0.40 rad、机械/速度/PD力矩边界。
- 当前`traced/`的`model_38300`成对资产哈希保持不变。seed 17、每高度20局、单箱
  长宽1.2 m、±5 mm地面、最长15秒，且启用出生、观测/深度、相机外参/FOV、摩擦、
  质量/质心、电机、推力和0～2周期延迟随机化时，0.25 m成功5/20，0.45 m成功
  4/20。两批分别有8/13次安全硬停；0.25 m硬停含7次速度越界和1次RR calf目标
  联合区间为空，0.45 m含9次速度越界、RR thigh两次/RL thigh一次联合区间为空及
  1次关节位置越界。
- 0.25 m的8799个策略周期中actor `±4.8`裁剪67周期、生产输出保护修改3402周期；
  持续约束2362周期，目标步长/机械/PD力矩分别命中1948/333/288周期。0.45 m的
  6348周期中actor裁剪750周期、输出保护修改3220周期；持续约束2180周期，三类
  分别命中1732/476/567周期。同一周期可同时命中多类，不能相加当作唯一裁剪周期。
- 结果JSON位于训练仓库`artifacts/onboard_model_38300_box0{25,45}_trials20_seed17.json`。
  该评测不覆盖DDS、Jetson循环、固件PD和真机动力学，20局单seed的成功率只描述本次
  随机样本，不能授权真实电机接管或替代承重支架验收。

## model_38300稳态请求步长分位诊断（2026-07-29）

- 在上述两批完全相同的闭环和随机样本上，只读记录稳态阶段每关节
  `abs(requested_q - previous_executed_q)`；不包含Y后首1秒的0.05 rad接入阶段，
  也没有修改生产限位。按四条腿同类关节取最坏分位，0.25 m的95%分位约为
  hip 0.076、thigh 0.300、calf 0.263 rad，99%分位约为0.118/0.412/0.416 rad；
  0.45 m的95%分位约为0.096/0.440/0.413 rad，99%分位约为
  0.212/0.797/0.619 rad。
- 若受当前实现约束、hip与thigh共用一个阈值，则只从“约95%的单关节请求不碰步长
  边界”看，0.25 m约为hip/thigh 0.30、calf 0.27 rad，0.45 m约为
  0.44/0.42 rad。这不是安全推荐：放宽后闭环轨迹会改变，且0.45 m样本已有速度、
  位置和目标不可行硬停；机械与PD力矩边界仍会独立裁剪。

## model_38300 hip/thigh稳态0.30 rad本地配置（2026-07-29）

- 用户基于稳态请求步长分位诊断，明确要求将hip/thigh稳态目标步长从
  `0.20`提高到`0.30 rad/周期`；calf保持`0.40 rad/周期`。Y后首1秒的
  `0.05 rad/周期`接入保护、相对起点`±0.3 rad`包络、机械限位、运行期速度检查和
  PD力矩可行区间均未改变。
- 该配置当前仅在本地仓库，尚未同步Jetson或驱动真机。此前20局评测结果来自
  hip/thigh 0.20、calf 0.40配置，不能作为0.30配置的成功率或安全证明；新配置必须
  重新执行随机化深度单箱闭环和承重支架验收。
- Python 3.8聚焦安全测试25项通过。CPU PhysX的`fixed/full`在±5 mm地面、0.2 m
  噪声块、seed 17下第394步越过20 cm单箱，0 reset、0 fault；四个thigh稳态最大
  步长均为0.30 rad，calf最大0.40 rad，hip最大0.1814 rad，最大预测PD力矩比例
  0.99796。NPZ位于
  `/home/tang/extreme-boundary-s2s/unitree-boundary-s2s-20260729-105655-346578.npz`。
  该确定性回放未覆盖0.25/0.45 m的20局随机化深度评测、Jetson、DDS或真机动力学。
- 相同seed 17、每高度20局、完整启动/物理/观测/深度随机化的复测已完成。0.25 m
  成功由5/20升至8/20，安全硬停由8降至3，目标步长裁剪率由22.14%降至5.98%；
  3次硬停为1次实测速度越界和2次RR calf联合可行区间为空。0.45 m成功由4/20降至
  3/20，安全硬停由13增至14，目标步长裁剪率由27.28%降至8.71%；14次硬停为13次
  实测速度越界和1次RR calf联合可行区间为空，最大速度比例从1.3577升至1.4706。
  因此0.30显著减少步长裁剪并改善低箱样本，但没有改善本批高箱成功率，也不能据此
  授权真机接管。结果位于训练仓库
  `artifacts/onboard_model_38300_step030_box0{25,45}_trials20_seed17.json`。
- 0.25 m随后以独立seed 18、其余条件不变再测20局：成功4局（20%）、安全硬停4局、
  接管前拒绝4局、超时8局。4次硬停均为calf实测速度越界（FL calf 3次、FR calf
  1次）；回放所选env 11为FL calf负向速度`-21.803 rad/s`，相对`20.07 rad/s`
  名义限值为`1.08637×`。4次接管拒绝均由随机初始关节速度超过低层接管阈值触发。
  8289个策略周期中任意裁剪率22.97%，目标步长/
  持续约束/机械限位/PD力矩命中率分别为7.84%/12.69%/3.47%/2.32%。与seed 17
  合并为40局12次成功（30%），合并任意裁剪率22.67%，因此不能把单批8/20的40%
  当作稳定成功率。新结果位于训练仓库
  `artifacts/onboard_model_38300_step030_box025_trials20_seed18.json`。
- `GO2_JOINT_VELOCITY_LIMITS`中的calf `20.07 rad/s`及配套`35.55 Nm`不是由真机
  固件读取，而是复制自训练仓库保留的早期Go2 URDF。Unitree官方仓库已在
  2023-10-11提交`79f054e`把四个knee/calf修正为`15.70 rad/s`和`45.43 Nm`；
  当前训练URDF仍是修正前值。两组数值对应关系发生反向变化，不能只更新其中一个，
  也不能在未核对具体Go2硬件版本、固件和厂家资料时把官方当前URDF直接写入真机
  边界；后续必须先建立硬件版本对应的权威参数来源。

## model_38300 calf稳态0.32 rad本地配置（2026-07-29）

- 用户要求观察降低calf步长后的效果，曾将calf从`0.40`改为`0.32 rad/周期`，
  hip/thigh保持`0.30`。接入期`0.05`、机械/速度/PD边界和旧URDF `20.07 rad/s`
  速度值均未改变。该实验配置仅在本地运行，从未同步Jetson或驱动真机。
- 用与0.40档逐环境一致的seed 17/18、完整启动/物理/观测/深度随机化复测0.25 m
  共40局：成功由12/40（30%）降至8/40（20%），安全硬停由7增至13，超时由17降至
  15，4次接管前拒绝保持不变。新13次硬停含9次实测速度越界和4次calf联合可行区间
  为空；旧7次为5次速度越界和2次联合区间为空。
- 合并16619个策略周期的目标步长/持续约束/任意裁剪率为9.85%/15.04%/26.66%，
  高于0.40档的6.83%/12.21%/22.67%；最大实测速度比例也由1.08637升至1.43238。
  因此固定缩小calf目标步长改变了闭环轨迹，但没有降低实测速度风险或提高成功率；
  不得将0.32描述为安全改进。用户随后要求回滚，当前生产同源calf稳态步长已恢复
  `0.40 rad/周期`，Python 3.8全套59项单元测试通过。结果位于训练仓库
  `artifacts/onboard_model_38300_step030_calf032_box025_trials20_seed{17,18}.json`。

## model_38300正装真机运行记录（2026-07-29 14:07）

- Jetson正装视觉节点于14:06:01启动且整轮没有相机错误；当前三份`traced`资产SHA-256
  与7月28日19:24的`model_38300`一致。Y于14:07:48.741进入policy，约1.007秒后完成
  接入，14:07:51.660由运行期保护以`measured joint position exceeded its limit`
  退出到站姿恢复，14:07:52.599用户按R2关闭电机。R2才触发飞行记录落盘，故NPZ的
  `reason`为`r2_emergency_stop`，但第一次退出policy的原因仍是此前的关节位置保护。
- 飞行记录含146个policy周期、约2.894秒，其中95个为稳态周期。最后一个被接受的
  policy样本中，FL calf实测角为`-2.769486 rad`；名义机械下限为`-2.7227 rad`，
  加入运行期`0.05 rad`容差后的拒绝线为`-2.7727 rad`，只剩`0.003214 rad`余量。
  该关节此前负向速度峰值为`-17.2559 rad/s`，最后样本仍为`-5.9124 rad/s`；下一
  个触发样本在校验异常后未写入记录，因此无法逐字取得其角度，但FL calf是本轮位置
  越界的明确首要候选。其命令目标为`-2.7000 rad`，未越过目标机械边界；故障机制是
  高速运动后的实测过冲，而不是控制目标直接下发到机械范围之外。
- 95个稳态周期中10周期的请求目标被修改：6周期由目标步长边界主导、6周期由预测PD
  力矩边界主导，两个集合有重叠；没有稳态周期由目标机械限位主导。全policy的61个
  修改周期还包含接入阶段51周期。最大请求/命令差为`0.533772 rad`。实测关节速度峰值
  为名义限值的85.98%，实际/预测PD力矩峰值分别为97.91%/100%，所以本轮日志报告的
  首次保护不是速度或力矩越界。
- LowState/深度年龄最大为19.09/57.70 ms，控制循环p95/max为12.53/13.63 ms，视觉
  日志无报错，电机`lost`计数在整段内不增长；相机、DDS输入和循环超时不是首要原因。
  进入policy前RR/RL hip温度已分别为81/91°C，恢复末尾RL hip达到93°C。现有保护只
  记录温度、不阻止接管；冷却并查明后髋高温原因前不得继续真机策略运行。
- 本轮FR足端在146个policy周期中100%保持接触，足力最低7，未跨过5的离地判据；
  FL和RL也100%接触，只有RR出现4段共22周期离地。15:58较好记录的前2.9秒中，四足
  接触率分别仅50.3%/42.8%/55.2%/60.7%，均有明确摆动，因此本轮确有“右前脚没有
  离地”的表现，但不是FR单腿独有现象。
- 按Go2 URDF两段0.213 m腿长重建相对机身足端高度，本轮FR命令的最大收腿幅度约
  13.54 cm，实测约12.76 cm；15:58同期分别约26.64/20.42 cm。本轮命令足端最高时
  目标/实测相对高度仅差约4 mm，且FR三个关节的平均跟踪误差与实际力矩比例都低于
  15:58对照。证据不支持“FR电机收到足够抬腿命令却没有能力执行”；更符合actor本轮
  给出的FR收腿目标不足，加上机身/接触闭环使FR仍擦地或承重。飞行记录未保存原始图
  和基座平移，无法仅凭该NPZ继续区分视觉内容与实际起点/箱距造成的策略差异。

## 飞行记录保存策略深度输入（2026-07-29）

- 用户要求后续日志能够直接查看策略深度图。生产`Go2Node`现在把每次实际传给
  `depth_encode()`的归一化`58×87 float32`输入加入既有50帧视觉环形缓冲；策略循环
  的一帧流水线保持不变，记录的是本次已编码的`last_depth_image`，不是刚收到、留给
  下一次编码的帧。
- 飞行NPZ升级为`format_version=3`，新增`depth_input(N,58,87)`；同一记录保留
  `visual_timestamp`、从该图直接计算的`depth_stats`和34维`visual_output`。无视觉
  样本时固定写出`(0,58,87)`，旧字段和无pickle读取方式保持兼容。默认50帧新增内存
  约1.01 MB，运行期只复制到内存，磁盘仍由既有原子flush路径一次性写入。
- 本轮不保存D435i原始`424×240 uint16`帧，也不修改ROS话题、视觉模型、深度预处理、
  策略或安全状态机。Python 3.8.10下5项聚焦测试和全套61项测试、相关语法检查及
  `git diff --check`通过；尚未同步Jetson，未连接ROS、相机或真机。
- v3代码随后已同步Jetson并于14:32启动视觉/控制节点，但14:34:37生成的
  `extreme-flight-20260729-143436-981309.npz`为0字节。磁盘仍有199 GB可用、目录中
  没有残留临时文件，故不是空间不足。Jetson实际在14:35:06重新启动，14:32建立的
  三个SSH会话均被`last`标记为`crash`；没有tee运行日志，ROS日志也为空，因此本轮
  控制、关节和深度数据无法恢复。现有`np.savez_compressed -> os.replace`只保证正常
  运行时原子替换，没有对临时文件和父目录执行`fsync`，不能把本次0字节文件描述为
  已通过掉电/整机重启耐久性验收。
- 静态复核不支持“v3深度记录导致整机重启”：新增环形缓冲约1.01 MB，flush只执行
  NPZ压缩与原子rename，项目没有`reboot`/`shutdown`调用；用户态压缩异常通常只会
  终止控制进程，不能解释三个SSH会话同时成为`crash`。7月28日10点也发生过早于v3
  的相似整机重启。0字节NPZ更可能是14:35重启打断或未持久化写入的结果，而非重启
  发起者。
- 14:35重启后的Jetson只读核查确认PMC `reset_reason=MAINSWRST`、`reset_level=L1`，
  当前boot的dmesg也明确打印`PMC reset source: MAINSWRST`。该分类是SoC软件复位，
  不同于Tegra标准看门狗的`BCCPLEXWDT`和硬件复位输入的`SYS_RESET_N`。但这不是正常
  systemd关机：`last -x`把14:32的三个会话标为`crash`，下次启动删除3个orphan inode
  并完成EXT4 recovery，且持久syslog/auth.log中没有reboot、shutdown或完整停止序列。
  同时没有上一boot journal、pstore panic、OOM、过热或看门狗记录，因此只能定位为
  软件/固件路径触发的非正常SoC复位，现有日志无法确定具体调用者。
- 只读排查期间Jetson又在14:50:38整机重启，PMC仍为`MAINSWRST`；当时没有运行ROS、
  D435i、策略、飞行记录或电机控制，仅在读取本机日志和service配置，因此这些机器人
  进程不是复位发生的必要条件。`last`还显示当天14:08、14:29、14:35和14:50连续多次
  启动，说明这是可复现的系统级故障。14:50重启前恰逢扫描大型syslog，但该时间相关性
  不能证明磁盘读取或负载是触发条件；新boot内存余量约13 GiB、温区约50～56°C，仍无
  OOM、过热、panic、pstore或正常关机证据。
- Jetson的Unitree任务源码包含收到`command == 'reStart'`后执行普通`os.system('reboot')`
  的旧路径，升级服务也常驻，但本次没有该任务节点进程、`reStart`日志、升级连接或
  升级运行记录，不能据此归因。`unitree-upgrade.service`只在收到WebSocket升级命令后
  调用`/unitree/services/install.sh`；脚本仅安装并重启Unitree services，不含整机重启
  命令，升级目录也没有近期文件活动。项目控制/视觉代码同样没有整机重启调用。精确
  追责需下一轮预先启用外部串口控制台，以及可持久化的reboot syscall审计或
  pstore/journal。

## model_38300正装0.25 m真机记录（2026-07-29 14:56）

- 本轮使用hip/thigh `0.30 rad/周期`、calf `0.40 rad/周期`。Y后共有159个被接受的
  policy周期、约3.153秒，其中接入51周期、稳态108周期；随后控制日志明确报告
  `measured joint position exceeded its limit`并进入站姿恢复，约0.62秒后用户按R2，
  所以NPZ的`reason=r2_emergency_stop`不是首次退出policy的原因。本轮没有整机重启，
  飞行文件正常保存到本机
  `/home/tang/.cache/extreme-runtime-logs/box025-20260729-145451/flight/`。
- 位置越界几乎可以锁定为FL calf。最后一个被接受的policy样本为实测
  `q=-2.733438 rad`、`dq=-4.689047 rad/s`，请求和命令均为`-2.700000 rad`；名义机械
  下限是`-2.7227 rad`，带0.05 rad运行期容差的拒绝线是`-2.7727 rad`。按本轮中位
  控制间隔20.25 ms做一周期线性外推约为`-2.8284 rad`，会越过拒绝线；其他关节离
  拒绝线至少0.538 rad。最后4周期FL calf actor输出为`-5.05～-5.69`并被actor
  `±4.8`裁剪，使请求固定在`-2.70 rad`，实测由`-2.024`快速冲到`-2.733 rad`。
  因而这是目标距离物理下限仅0.0227 rad时的动态过冲，与calf `0.40 rad/周期`稳态
  步长本身无直接关系；最后几帧该目标也没有命中机械目标边界或PD力矩边界。
- 108个稳态周期中21周期的请求目标被输出保护修改：目标步长命中16周期、预测PD力矩
  命中6周期、目标机械限位命中0周期，同一周期可重叠；最大请求/命令差0.77394 rad。
  最大实测速度为FL calf名义值的71.90%，实际/预测PD力矩峰值为97.80%/100%，控制
  循环p95/max为11.62/13.93 ms，LowState/深度最大年龄19.65/35.43 ms，电机`lost`
  全0且最高温度56°C。速度、输入时效、循环超时、高温和新增丢包都不是首次故障。
- policy期间roll/pitch范围为`-13.30°～-0.63°`/`-16.25°～+3.62°`。FR、FL、RL足力
  从未低于接触阈值，只有RR离地29周期；基于两段0.213 m腿长的近似机身坐标重建，
  FR命令/实测最大收腿约14.12/13.39 cm，FL约24.77/21.83 cm。该结果继续支持
  “FR得到的收腿目标不足”，不支持“FR收到足够目标但电机无法跟踪”；但足端接触可能
  是地面也可能是箱体，NPZ没有基座平移和同步视频，不能仅据此还原是否碰到箱沿。
- v3共保存40帧实际策略深度，其中policy覆盖31帧，间隔中位数99.98 ms、最大
  101.77 ms，无冻结。反归一化后的帧均值约0.825～1.238 m，箱体从Y前清晰居中逐渐
  逼近，约policy `+2.5 s`后近距离表面占据大部分画面，末两帧出现近边缘混合像素；
  画面方向正确，没有180°倒置证据。内核在policy约`+2.42 s`出现一次UVC `GET_CUR`
  warning，但视觉周期和深度年龄没有中断，不能把它作为本轮动作或保护退出原因。

## 14:56记录与7月28日15:59记录对比

- 按Y后的墙钟时间对齐后，昨天的飞行环形缓冲从约`Y+1.68 s`才开始，不能比较接入
  阶段。可直接对比的`Y+[1.70,3.15] s`内，昨天73周期、今天72周期；昨天使用全关节
  `0.20 rad/周期`步长，今天使用hip/thigh `0.30`、calf `0.40 rad/周期`。
- 两次在该窗口的关节跟踪误差几乎相同：昨天/今天p95为`0.500/0.502 rad`，各腿最大
  误差也相近；深度空间均值中位数为`0.903/0.900 m`。因此没有证据支持今天主要由
  电机跟不上或整体起始箱距不同造成。昨天未保存原始深度图，仍不能排除横向偏移、
  裁剪和相机yaw等空间差异。
- 关键分叉在FL calf。昨天它同样一度到达`q=-2.7607 rad`，比今天最后一个接受样本
  `-2.7334 rad`更靠近运行期拒绝线`-2.7727 rad`；但昨天actor从约`Y+3.04 s`开始
  回撤，命令由`-2.70`回到`-2.62 rad`，关节速度在约40 ms内反向，从而逃离下限。
  今天actor约晚100 ms才开始强收腿，并在末4周期持续输出`-5.05～-5.69`，请求一直
  饱和在`-2.70 rad`，没有及时回撤，下一周期因动态过冲触发位置保护。
- 今天更宽的calf `0.40 rad/周期`步长使输出保护的缓冲更弱：同期被保护修改的周期为
  15/72，昨天为34/73；但它不是唯一根因，因为两次最终都到达`-2.70 rad`且都有较高
  负速度，决定是否越界的是actor随后是否及时撤回。
- 同期足接触率昨天FR/FL/RR/RL为`31.5%/65.8%/79.5%/75.3%`，今天为
  `100%/100%/79.2%/100%`。只有RR相近；昨天已经在进行明显的多腿卸载/摆动，今天
  除RR外持续承重，说明策略闭环在接近箱体时已经走到不同状态，而不是单个关节故障。
- 昨天的“表现好”只适用于越箱前半段：它没有在3.15秒位置保护，而是运行到
  `Y+6.66 s`后由R2停止；但后半段roll最高约`75.1°`、四足全离地50周期、RR calf
  `lost=7`且实际力矩峰值约111%名义值，不能视为稳定成功样本。

## calf下限过冲的训练根因（2026-07-29）

- `model_38300`训练/导出契约允许calf目标到`-2.70 rad`，距离URDF下限
  `-2.7227 rad`只有`0.0227 rad`。训练配置的`soft_dof_pos_limit=0.9`仅计算软范围；
  reward scales没有`dof_pos_limits`且代码没有对应奖励函数，episode termination也
  不检查关节位置或速度，因此策略没有学到高速接近下限时提前回撤，而是可以依赖
  PhysX关节止挡。全关节`dof_error`弱惩罚不能替代单独的限位/制动距离约束。
- 仿真会发生名义机械下限过冲。既有0.45 m确定性S2S记录的四个calf最小角为
  `-2.74354/-2.74083/-2.75133/-2.72816 rad`，均低于或接近`-2.7227 rad`，最深
  超出`0.02863 rad`；旧0.20/0.40随机化0.45 m的20局中还有1局直接触发
  `measured joint position exceeded its limit`。当前0.30/0.40的0.25 m seed 17/18
  共40局没有位置硬停，但该批未复现不等于无过冲。真机14:07和14:56的FL calf故障
  是同类机制叠加未建模的固件PD、传动弹性/间隙、接触冲击、时延和标定误差，不应把
  部署端位置保护或单周期`0.40 rad`步长本身认作根因。

## 复现7月28日15:59的稳态步长配置（2026-07-29）

- 用户要求先恢复7月28日15:59真机测试的目标步长，用于观察能否复现当时行为；当前
  本地配置将hip、thigh和calf的稳态目标步长统一恢复为`0.20 rad/周期`。Y后首1秒
  `0.05 rad/周期`接入限制、相对起点`±0.3 rad`包络、机械限位、速度检查和PD力矩
  可行区间均未改变。
- 本次只修改本地仓库，没有同步Jetson、启动ROS/相机或发布真实LowCmd。Python 3.8
  聚焦安全测试25项及`tests/`全套61项通过；该回退只建立复现条件，不构成真机安全
  结论，且可能重新出现前腿calf收腿不及时碰箱沿的问题。

## 稳态0.20配置最近两轮真机记录（2026-07-29 16:27/16:31）

- 用户随后纠正实验标签：倒数第二轮`step020-20260729-162632`是0.25 m完全成功，最后
  一轮`step020-20260729-163054`才是0.45 m失败；不得继续沿用此前把16:27记录标为
  0.45 m失败的结论。0.25 m飞行文件为
  `flight/extreme-flight-20260729-162748-520219.npz`，Y到R2约7.96秒；250周期环形
  缓冲只覆盖约`Y+2.98～7.96 s`，因此不包含最初接近阶段。
- 0.25 m成功轮在最近250周期中有152周期请求被修改。排除缺少上一目标的首行后，
  步长、预测PD力矩和目标机械边界分别主导141、27和11周期，可重叠；全关节实际最大
  命令步长均为`0.20 rad/周期`。actor有21周期超过`±4.8`裁剪线。该配置通过频繁整形
  仍完成任务，不能把“成功”理解为策略原始输出本身平滑或远离边界。
- FR/FL calf分别有3个实测样本越过名义`-2.7227 rad`下限，最小值为
  `-2.76350/-2.75884 rad`，距运行期拒绝线`-2.7727 rad`仅约0.0092/0.0139 rad；两者
  随后及时反向，没有触发位置保护。最大实测速度为名义值72.3%，没有速度越界或
  `lost`增长。
- 越箱高负载段约在`Y+5.50～5.60 s`：roll/pitch达到`-21.55/+29.27°`，FL足力峰值
  100，RR处于离地，FL calf命令/实测误差峰值0.967 rad。实际`tau_est`共11周期超过
  名义力矩，峰值为FL thigh的109.9%；预测PD力矩仍被输出边界限制在100%。机器人随后
  恢复到R2前约`roll=+0.42°、pitch=-10.94°`，所以这是高负载、低余量的物理成功。
- 视觉记录完整保存50帧策略输入，间隔中位数约99.8 ms、最大约102.4 ms，深度年龄
  最大34.9 ms，无冻结。约`Y+5.5～6.3 s`近距离表面占据大部分画面，与跨上/越过箱体
  的高姿态阶段同步；之后重新看到远端地面，画面方向正常。
- 0.45 m失败目录只有103字节相机启动日志和被NUL截断的控制日志；控制日志可恢复内容
  只到16:31:33的MotionSwitcher检查，`runtime-status.log`为0字节且没有飞行NPZ。
  因此这轮没有任何可用的policy动作、关节、足力或深度样本，不能从现有文件判断失败
  关节或安全限位。目录在16:31:38～16:31:41停止更新的形态与异常重启打断写回一致，
  但尚未读取该次启动后的PMC reset reason，不能仅凭实验文件确认具体复位来源。

## 稳态0.20配置目标不可行急停记录（2026-07-29 19:05）

- 最新`step020-20260729-190420`记录完整保存；Y后198个policy周期、约3.93秒，首次
  退出原因是`policy_target_infeasible`，约0.69秒后用户才按R2。故障处理没有发布被
  拒绝的目标，而是直接执行motor-off并以该原因保存飞行NPZ。
- 用户现场确认机器人没有摔倒：机身以及FR、FL、RL均已上箱，只有RR未上。因而约
  `Y+2.8 s`后增大的roll/pitch不是倾倒证据，而是三腿和机身在箱上、RR留在箱下的
  跨台阶姿态；约`Y+3.28 s`的pitch为`-41.0°`，最后接受样本roll/pitch为
  `+35.2/-32.7°`。策略深度从约`Y+3.2 s`开始接近全场2 m远裁剪，也应解释为相机
  随该大姿态越过箱面后看向远处，不能据此认定视觉故障或摔倒。
- actor在`Y+2.8 s`前没有超过`±4.8`，之后57周期中有43周期被裁剪；最后37周期的
  请求全部被输出保护修改。147个稳态周期中99周期被修改，目标步长、预测PD力矩和
  目标机械边界分别主导95、14和20周期，可重叠。这说明RR上箱失败阶段策略在快速
  切换目标，而0.20步长和其他边界持续整形；不能再把该段描述为倾倒恢复。
- 四个calf都短暂越过名义`-2.7227 rad`下限，最深约0.0383 rad，但没有越过带0.05
  rad容差的拒绝线；最大实测速度为名义值85.3%，实际力矩峰值为RR thigh的108.9%，
  `lost=0`、最高温度47°C、LowState/深度年龄最大19.3/38.0 ms。位置、速度、输入过期、
  高温、丢包和视觉冻结均不是本轮首次退出原因。
- 现有`PolicyTargetInfeasibleError`包含不可行关节索引，但`_handle_policy_fault()`只
  记录归一化reason，丢弃`str(error)`；飞行记录又只在命令成功后写样本，因此触发
  周期的关节状态、请求和边界也没有入NPZ。最后一个接受样本及其一周期线性/加速度
  外推仍可行，故不能从当前文件确定具体关节。结合现场观察，RR thigh成为最强候选：
  RR足端在`Y+3.71 s`已收到相对机身约`-0.068 m`，`Y+3.77 s`足力短暂升到33，随后
  又下伸到约`-0.332 m`且足力降到6；最后RR thigh请求为`-0.20 rad`，受0.20步长限制
  只下发到`+0.295 rad`，实测仍为`+0.972 rad`并以约`-12.0 rad/s`运动。该快速反向及
  边缘接触很可能使下一周期步长与PD力矩区间失交，但在记录具体索引前仍属高可信候选，
  不是已证实归因。
- 上述日志缺口随后已修复。`PolicyTargetInfeasibleError`现在保留结构化关节索引，并在
  异常文本中逐关节记录requested、previous、measured q/dq、目标步长区间、机械区间、
  PD力矩导出的目标区间和最终空交集；`_handle_policy_fault()`会把actor顺序关节名和
  完整异常文本写入同一条急停日志。控制约束、急停方式和飞行NPZ schema均未改变。
  两个运行文件已在确认Jetson没有相关节点后同步，远端与本地SHA-256一致；本地及
  Jetson的25项安全测试和相关语法检查通过。
- 使用19:05记录的相同请求和实测状态离线重放hip/thigh `0.20/0.25/0.30`、calf保持
  `0.20 rad/周期`时，147个稳态周期中请求被修改的周期为99/91/87。故障前最后一个
  RR thigh样本中，0.20配置下发`+0.295 rad`，0.25和0.30都因PD力矩边界只能下发到
  同一个`+0.080 rad`，预测力矩达到`-23.7 Nm`名义下限；因此0.30在该关键样本不比
  0.25提供更多目标空间，只增加其他阶段的目标跳变许可。该重放固定了真实实测轨迹，
  不能预测改动后的闭环结果；若必须先做小步真机A/B，优先hip/thigh 0.25、calf 0.20。

## 19:30真机实验异常复位记录（2026-07-29）

- `step020-20260729-193002`只保存了`camera.log`、`control.log`和
  `runtime-status.log`；`flight/`为空，没有产生NPZ。两份控制文本只持久化到订阅
  LowState/遥控器以及`/lowcmd`尚未建立的初始化阶段，文件尾部被NUL填充。
  因此这轮没有可用的policy、关节、足力或深度帧数据，新增的不可行关节日志
  也没有执行或持久化的机会。
- Jetson本轮实验后的启动时间为`2026-07-29 19:31:15`；PMC报告
  `reset_reason=MAINSWRST`、`reset_level=L1`，`last -x`将实验期间的SSH会话标记为
  `crash`，当前启动内核又报告3个orphan inode被删除且ext4 recovery complete。
  这足以确认日志由整机突发复位中断，但`MAINSWRST`本身不能识别是哪个软件、
  固件或硬件路径发起了复位。
- 远端目录已回传至本地
  `/home/tang/.cache/extreme-runtime-logs/step020-20260729-193002`；3个文件的本地与远端
  SHA-256逐一一致。

## 稳态步长改为hip/thigh 0.25（2026-07-29）

- 用户选择进行真机A/B；已将hip和thigh的稳态目标步长从`0.20`改为
  `0.25 rad/周期`，calf保持`0.20 rad/周期`。Y后首1秒`0.05 rad/周期`接入限制、
  相对起点包络、机械限位、实测速度检查和PD力矩可行区间均未改变。
- 运行时文件和对应测试已在确认Jetson无控制器/相机进程后同步，本地与
  Jetson的SHA-256一致。Jetson端25项安全测试全部通过，语法检查通过；
  实际关节向量为`(0.25, 0.25, 0.20) × 4`。

## hip/thigh 0.25首轮真机异常复位（2026-07-29 19:41）

- 最新目录`step020-20260729-194132`中，控制器于19:42:08完成Sport Mode释放、
  建立`/lowcmd`并打印`Model and Policy are ready`；然而`flight/`为空，没有
  NPZ，文本日志也没有持久化Y后policy周期或关节故障行。
- Jetson随后于19:42:42重新启动，PMC仍报告`MAINSWRST/L1`，SSH会话被
  `last -x`标记为`crash`，当前启动中有3个orphan inode删除和ext4 recovery。
  未发现OOM、panic、thermal或watchdog触发记录；但设备未开启持久化journal，
  `journalctl -b -1`无法提供前一次启动的尾部日志。`MAINSWRST`仍不能定位复位发起者。
- 目录已回传至
  `/home/tang/.cache/extreme-runtime-logs/step020-20260729-194132`，3个文件的本地与
  Jetson SHA-256逐一一致。

## 抗整机复位飞行记录（2026-07-29）

- 连续出现`MAINSWRST/L1`后，已将飞行记录从“只在L2/R2/异常/退出时清盘”改为
  首个控制样本立即启动后台检查点，之后每0.5秒最多一次。检查点先写临时
  NPZ，再`fsync(file) -> os.replace -> fsync(directory)`；复位打断当前写入时，
  上一份完整检查点仍保留。正常清盘只在最终NPZ耐久落盘后才删除检查点，
  写入失败则保留检查点和内存环形缓冲。
- NPZ升级为`format_version=4`，新增`detail`且保留稳定`reason`。周期检查点
  使用`reason=periodic_checkpoint`和空`detail`；软件急停的最终NPZ会将具体关节、
  requested/previous/measured q/dq及步长/机械/PD力矩区间写入`detail`，不再只依赖
  易丢尾的`control.log`。后台错误只限频告警，不改变任何控制或急停语义。
- 本地10项飞行记录和25项安全测试通过；Jetson端66项全套测试与语法检查
  通过，三个同步文件的本地/远端SHA-256一致。Jetson代250周期/50帧生成
  928661字节检查点耗时93.844 ms（后台执行）；5秒50 Hz合成循环中记录调用
  p50/p95/max为0.224/0.684/2.415 ms，0超期。子进程被`SIGKILL`后仍从v4检查点
  恢复76个控制样本和16帧视觉样本。该验证未连接ROS/相机/真实LowCmd，未主动重启Jetson。

## 稳态步长改为hip/thigh 0.21（2026-07-29）

- 按用户要求，本地hip/thigh稳态目标步长已从`0.25`改为`0.21 rad/周期`，
  calf保持`0.20 rad/周期`，实际向量为`(0.21, 0.21, 0.20) × 4`。首1秒
  `0.05 rad/周期`接入限制、机械限位、速度检查、PD力矩区间及抗复位日志均未改变。
- Jetson恢复网络后，已在确认无控制器/相机进程时，将`0.21`配置、抗复位
  NPZ v4记录器、控制节点和两份对应测试共5个文件整组重传。本地/远端
  SHA-256逐一一致；Jetson端35项聚焦测试和66项全套测试全部通过，语法检查通过。
  运行时实读为`format_version=4`、检查点间隔`0.5 s`、接入步长`0.05`，稳态
  向量`(0.21, 0.21, 0.20) × 4`。

## 0.21首轮v4飞行记录（2026-07-29 20:15）

- `step020-20260729-201529`成功生成797648字节的最终NPZ v4，包含250个控制样本
  和50帧视觉样本。`reason=policy_target_infeasible`，新增`detail`已按设计保存
  `RR_thigh_joint`的触发周期：requested=-0.200000、previous=-0.013357、
  measured q/dq=+0.796217/+2.600477，步长区间[-0.223357,+0.196643]，
  PD力矩目标区间[+0.268729,+1.453729]，最终交集[+0.268729,+0.196643]为空。
  这证明软件急停最终NPZ和详细故障字段生效，但本轮不是整机复位，因而
  不单独构成`checkpoint.npz`抗`MAINSWRST`的真机验收。
- 最新目录已回传至
  `/home/tang/.cache/extreme-runtime-logs/step020-20260729-201529`。NPZ可由NumPy完整读取；
  camera/control/runtime-status/NPZ共4个文件的本地与Jetson SHA-256逐一一致。
  检查时Jetson启动时间仍为19:48:35，中途`No route to host`为网络瞬断，不是新的整机重启。
- Y后保存了213个policy周期，最后接受样本为`Y+4.233 s`，急停日志为
  `Y+4.266 s`。首51周期接入段全部被过渡保护修改；162个稳态周期中89周期
  请求被修改（54.9%），步长/机械/PD力矩请求命中分别为87/17/23周期，可重叠。
  actor在27个稳态周期超过`±4.8`（16.7%），最大为RR thigh的`-10.105`；最大实际
  命令步长精确为`0.210000 rad`。
- RR thigh在`Y+4.01～4.13 s`的足力从1/3升至22/56/65/48/9，随后降为0。同时actor
  将该关节raw action从`+2.207`快速反向到`-10.105`，requested target连续六周期在
  `-0.200 rad`动作裁剪线饱和；下发目标从`+1.552`降到`-0.013 rad`。最后接受周期
  实测q/dq为`+0.709/-18.060`，预测PD力矩仍为`-10.82 Nm`。下一触发周期实测突变为
  `+0.796/+2.600`，上一目标的预测力矩因位置滞后和速度反向跳到约`-34.98 Nm`。
  为保持力矩不低于`-23.7 Nm`，目标必须不低于`+0.268729`；但从previous
  `-0.013357`按0.21步长最多只到`+0.196643`，缺口为`0.072086 rad`。请求`-0.200`
  本身在步长区间内，空交集的直接成因是回弹后的PD力矩下界超过步长上界。
- 固定该触发状态反算，hip/thigh步长至少需`0.282086 rad`才有非空交集；0.25仍会失交，
  0.30会得到`[+0.268729,+0.286643]`可行区间。但这会强制下发与actor的`-0.200`
  相反的正向目标，只证明静态数学可行，不证明0.30闭环真机更安全。
- 本轮不是位置、速度、输入过期、丢包或视觉冻结触发：最大速度为标称值63.1%，
  LowState/深度年龄最大约19.2/59.5 ms，`lost`无增长，42帧Y后视觉输入间隔最大
  103.5 ms且无重复帧。循环p95/max为12.57/15.58 ms，检查点记录没有造成20 ms超期。
  实际力矩最大为FL calf的99.2%；三个calf共6周期越过名义下限0.034～0.041 rad，仍在
  0.05 rad容差内。RL hip/RR hip温度恒为70/65°C，不是本轮急停原因，但较先前
  47°C记录明显更高，后续真机前应独立关注。
- 与19:05的0.20轮相比，本轮接受policy时间从3.930增至4.233秒，稳态修改比例从
  67.3%降至54.9%，actor裁剪比例从29.3%降至16.7%，但两轮接触序列、姿态、温度和
  实测轨迹都不同，不是受控A/B，不得将全部差异归因于0.01 rad步长改动。两轮均在
  RR thigh快速反向附近以`policy_target_infeasible`结束，新v4记录首次将该关节实证。

## hip/thigh条件式PD力矩安全逃逸（2026-07-29）

- 用户确认足端卸载后的快速回弹属于预期动作，同意先按条件式方案放开：普通稳态
  hip/thigh仍为`0.21 rad/周期`、calf仍为`0.20 rad/周期`，不是把全程步长直接改成
  0.30。只有上一目标按最新q/dq预测已超名义PD力矩、普通步长与安全目标失交、
  机械/PD力矩安全区间和0.30步长仍有交集，且候选更接近实测q并降低预测力矩绝对值时，
  对应hip/thigh才可使用最大`0.30 rad/周期`。首1秒0.05接入和calf不启用逃逸；任何
  剩余无解继续按原路径motor-off。
- 20:16:44的`RR_thigh_joint`故障状态离线精确重放后，安全输出为
  `+0.268728925 rad`，相对上一目标步长`+0.282085925 rad`；预测PD力矩从
  `-34.983437`降至名义边界`-23.700000 Nm`。这不是放行actor请求的`-0.200000`，
  而是越过普通步长进入PD力矩安全区间。
- Python 3.8.20下70项全套测试、相关语法检查及`git diff --check`通过；新增测试覆盖
  精确RR thigh状态、接入期禁用、calf禁用和0.30仍不足时拒绝。控制节点会在实际逃逸
  后记录关节名、previous/commanded/measured q/dq和逃逸前后预测力矩。
- 用户离开现场后停止了Jetson连接；本次改动尚未同步Jetson，未启动ROS、D435i或
  真实LowCmd，也不是真机安全验收。下次必须先确认远端无控制/相机进程，再同步
  `real_control_safety.py`、`unitree_ros2_real.py`和对应测试并运行远端全套测试。

## 条件式力矩逃逸Isaac闭环A/B（2026-07-29）

- 使用训练仓库的Onboard完整边界单箱入口、当前`traced/model_38300`、真实Isaac深度
  相机、启动及Sim-to-Real随机化，对“逃逸关闭”和“稳态条件式逃逸开启”做同seed 17、
  每组20局A/B。旧模式不传`escape_max_step_rad`，与修改前路径等价；新模式只在稳态
  传`(0.30,0.30,0.20) × 4`，首1秒接入不变。
- `0.25 m`两组均为成功8/20、安全硬停5、超时7，逃逸0次；5次硬停全为实测速度
  越界，因此该批没有复现20:16真机RR thigh空交集。
- `0.45 m`旧组在20局中有5局严格出现`RR_thigh_joint`普通0.21步长与PD力矩区间
  失交，所需步长为`0.244464/0.310337/0.261843/0.268875/0.234695 rad`。新版按设计
  对4个不超过0.30的周期逃逸并把预测力矩降到`-23.7 Nm`；0.310337局继续急停。
  4个逃逸trial最终为1成功、1超时、2次更晚的速度硬停，证明解除当前空交集不保证
  整局成功。
- 0.45 m总体旧/新为成功`0/20 -> 3/20`、安全硬停`17 -> 14`、超时均3；向量化仿真
  在首个逃逸后会改变后续共享随机数消费，只有1个新增成功trial自身发生过逃逸，不得
  把总体差异全部解释为逃逸收益。四份JSON保存在训练仓库`artifacts/`；未连接Jetson、
  ROS、D435i或真实LowCmd，生产改动仍尚未同步Jetson。
- 另用Onboard `HEAD db61c2d`临时工作树中的真实改动前安全源码重跑0.45 m，其
  summary、20条trial和随机化与当前源码显式关闭逃逸组逐值一致；旧/新安全模块
  SHA-256分别为`413136b8...`和`d677f0cb...`。验证后已移除无修改的临时工作树。
- Isaac Gym训练仓库46项测试和Onboard Python 3.8.20全套70项测试通过；相关语法、
  JSON有限值/计数完整性和两个仓库的`git diff --check`均通过。
- 当前逃逸代码0.45 m参考录制保存在训练仓库
  `artifacts/onboard_current_box045_escape_env12_seed17.mp4`。视频为20环境并行Viewer、
  跟随env 12，记录其policy周期148的RR thigh逃逸和周期340成功；规格为1600×900、
  50 FPS、522帧、10.44秒。用户确认保留20狗画面即可；该视频不是真机验收。
- 同一seed、随机化、20环境和env 12视角的原代码完整对照视频保存在训练仓库
  `artifacts/onboard_original_box045_rrthigh_fault_env12_seed17_full.mp4`。录制实际加载
  本仓库`HEAD db61c2d`且未启用力矩逃逸；env 12在policy周期148因
  `RR_thigh_joint`普通步长上界`+0.294832 rad`低于PD力矩目标下界`+0.346675 rad`
  而安全停止，随后以安全待机目标继续录到全部20环境结束。视频为H.264、
  `1600×900`、50 FPS、932帧、18.64秒。不带`_full`的6.60秒文件是故障点短片，
  不是完整回放；该仿真对照也不是真机安全验收。

## 当前限位详细归因与运行时候选淘汰（2026-07-29）

- 当前`a5abebc`边界、`model_38300`、seed 17、每高度20局的详细复测中，0.25 m为
  8成功、5安全硬停、7超时；4次硬停是actor索引5 `FL_calf_joint`负向速度越界，
  1次是索引11 `RL_calf_joint`。0.45 m为3成功、14安全硬停、3超时；其中11次
  `FL_calf_joint`负向速度越界、1次`RR_calf_joint`负向速度越界、1次RR calf位置
  越上界容差、1次RR thigh需要`0.310337 rad`而超过0.30力矩逃逸上限。速度硬停
  阈值为`20.07×1.005=20.17035 rad/s`，最深实测为`-29.717289 rad/s`。
- `validate_policy_runtime_inputs()`现在在保持原阈值和停止语义不变的情况下，逐关节
  输出位置/速度实测值、名义限位和含容差阈值。actor索引顺序必须按
  `FR/FL/RR/RL`解释，因此索引5是FL calf，不能标成FR calf。
- 运行时目标制动在calf已经抵达机械目标上界时会要求越过机械边界，0.25 m实验20局
  全部变成目标不可行。通过等效PD目标把Kd×1.2真实施加到PhysX后，0.25 m为
  `9成功/2硬停/9超时`，但0.45 m恶化为`0/19/1`；只对FL calf负向速度提高阻尼，
  0.45 m仍为3成功且硬停增至17。候选控制分支、CLI和计数已全部回滚，生产边界只
  保留详细诊断。
- 根因修复转到训练仓库的速度/软位置奖励。当前traced权重不会因训练配置修改自动
  改善；必须微调或重训、成对导出base/vision权重，再通过相同Onboard闭环评测。
  本轮未连接Jetson、ROS、相机或真实LowCmd。

## V3真机候选独立部署（2026-07-30）

- V3按用户确认的候选定义制作：以`a5abebc`和当前未提交的详细位置/速度故障日志为
  基础，只将四个calf的policy运行期名义速度限位从`20.07`改为`21.32 rad/s`；现有
  `0.5%`容差对应实际触发线`21.4266 rad/s`。其他速度/位置/步长/PD力矩与条件式逃逸
  规则不变。
- 候选已独立同步到Jetson `/home/unitree/Extreme-Parkour-Onboard-V3`，没有覆盖现有
  `/home/unitree/Extreme-Parkour-Onboard`，也没有启动ROS、相机或真实LowCmd。远端70项
  Python测试和相关语法检查通过，源码、traced权重及配置哈希与上传源一致。
- 该候选的seed 17仿真有收益但seed 18未显示稳定收益，仍不是已验收的生产安全边界；
  远端目录内`V3-CANDIDATE.md`记录了范围和风险。
- 用户随后要求先做V1-Log。Jetson活动目录`/home/unitree/Extreme-Parkour-Onboard`
  已同步为`a5abebc`控制行为加详细运行期位置/速度故障日志：hip/thigh普通步长`0.21`、
  calf步长`0.20`、条件式hip/thigh逃逸上限`0.30 rad/周期`，calf速度仍为
  `20.07 rad/s`。同步前文件保存在
  `/home/unitree/Extreme-Parkour-Onboard-backups/pre-v1log-2oNGoOB4`；远端70项测试与
  相关语法检查通过，traced资产未变化，也没有启动ROS、相机或真实LowCmd。

## V1-Log首次真机检查点（2026-07-30）

- 运行目录`v1log-20260730-103542`在Jetson非正常重启后留下0字节`controller.log`、
  仅含D435i启动行的`visual.log`，以及681257字节、format v4的耐久检查点。检查点已
  拉回本机`/home/tang/extreme-runtime-logs/v1log-20260730-103542/flight/`；它包含
  重启前最后250个50 Hz控制周期和50帧实际策略深度输入，证明0.5秒原子fsync路径在
  本轮存活，但不能提供检查点之后的终止异常文本。
- 检查点250周期全为稳态policy、跨度4.985秒；所有动作、状态、深度和视觉输出有限，
  LowState/深度最大年龄23.09/62.55 ms，循环p50/p95/max为8.84/13.04/18.25 ms，
  50帧深度无相邻重复。电机`lost`全0，最高温度51°C。
- 保存窗口内没有位置或速度运行期越界；calf最大速度为FL的13.151 rad/s，仅为
  20.07名义值的65.5%，因此V3的21.32限速不会改变这段轨迹。最接近位置触发线的是
  FL calf `q=-2.771824 rad`，距含0.05容差的`-2.772700`仅0.000876 rad，但仍未触发。
  普通输出约束修改121/250周期，最大命令步长0.210000 rad，未见超过普通步长的条件
  式力矩逃逸；raw action有50周期超过±4.8。
- 末段姿态达到roll范围`-15.11°～+31.56°`、pitch范围`-43.83°～+41.58°`，最终为
  `-15.11°/+41.58°`；最终仅RR足接触判定为0，整个窗口RR接触率74%，其余三足接近或
  等于100%。RR thigh实际估算力矩峰值`-25.926 Nm`、为23.7名义值的109.4%，同时静态
  PD预测被边界夹在`-23.7 Nm`，说明真实估算力矩可越过当前预测模型。
- Jetson随后整机重启且SSH会话被`last`标为crash；没有正常flush NPZ或可用ROS文本
  报错，不能从当前证据认定是policy保护触发、控制进程异常还是外部软件/固件复位。
- 用户现场确认机器人已成功上到箱顶，故障发生在下箱阶段。该观察与检查点存在明确
  时序对应：保存窗口前2.2秒roll约`-4.47°～+0.74°`、pitch约`-9.91°～-1.17°`，
  FR/FL/RL持续接触且RR为正常摆动，深度图持续显示近距离平台表面，支持箱顶稳定行走。
  在相对时间2.399秒，相邻策略深度帧平均绝对变化达到1.246 m，`<1 m`像素比例从
  75.7%骤降到9.7%，`>1.8 m`比例从22.7%骤升到86.5%，是近处箱顶平面越过视野边缘
  后看到下方/远处空间的直接视觉证据。
- 边缘事件前后RR先在2.300秒失去接触，2.401秒脚端力冲到161，机身pitch从
  `-13.8°`继续到`-43.8°`、roll随后到`+31.6°`；FL calf同时伸到`-2.771824 rad`，
  RR thigh电机估算力矩随后达到名义值109.4%。2.2～3.2秒的51周期中50周期被输出
  边界修改、42周期raw action裁到±4.8，说明下箱/冲击阶段策略和保护均处于极限动作区。
- 3.2～4.5秒姿态曾恢复到roll约`-2.5°`、pitch约`-6°`且四足重新接触，但4.6秒后
  pitch在约0.38秒内从0升到`+41.6°`、roll降到`-15.1°`，最终RR再次失去接触，构成
  下箱落地后再次失稳并摔落的证据链。记录没有基座世界坐标，不能单独从NPZ量出箱顶
  高度，但现场观察、深度边缘、冲击和姿态时序相互一致。

## V1-Log前脚落点时机复核（2026-07-30）

- 用Go2 URDF两段`0.213 m`腿长和实测/请求/执行关节角重建足端相对髋关节的前后
  位置。0.45 m失败记录在越沿前0.2秒至后0.8秒内，FR/FL执行目标最大向前仅
  `3.8/11.7 cm`；已确认成功的0.25 m记录同类窗口约为`14.8/18.4 cm`。高度不同，
  该对照只能证明本轮首次落脚的前向放置明显更短，不能单独证明因果。
- 这不是安全层把充足的前伸命令统一裁短：首次下箱窗口FR/FL的actor未裁剪足端最大前伸
  约为`2.8/11.7 cm`，最终执行最大值为`3.8/11.7 cm`。虽然部分周期输出边界会改变足端
  前后位置，但偏短的首要来源是actor本身在该时段没有更早给出大幅前伸。
- 用户确认画面右侧存在真实物体，它可能被视觉策略当作箱体。该物体约在相对`2.6～2.7 s`
  进入右侧视野；随后FL请求足端从前方约`+5 cm`快速收到`-18 cm`，到`2.8 s`FR/FL约为
  `-18/-26 cm`。时序支持视觉干扰是候选因素，但姿态和接触反馈同时变化，单次记录
  不能建立反事实因果。
- 第二次失稳开始前约`4.462 s`，RR先失去接触，此时FR/FL实测足端都在髋后约`-9.7/-10.0 cm`。
  之后FL和FR分别延迟至约`4.6/4.7 s`才向前到`15 cm`量级；整段最大前伸`15～16 cm`、实测
  摆幅`24～29 cm`，所以不支持“腿的最终前伸能力不足”，更符合“RR支撑丢失后前脚前放
  来得太晚”。FR/FL全窗口接触率为`99.2%/100%`，但成功0.25 m记录的FL也持续触地，
  因此足力阈值不能单独把它判成异常拖脚。
- 可视化保存在本轮`analysis/front_foot_stride.png`和`analysis/depth_front_target_alignment.png`。下一个最小
  因果实验是保持权重、安全边界、箱高和起点不变，只移走右侧物体，对比下箱后前1秒
  FR/FL前伸时机与RR首次失去接触的顺序。

## 新相机支架静态深度检查（2026-07-30）

- Jetson重新枚举到`8086:0b3a Intel RealSense Depth Camera 435i`，抓帧前没有视觉或控制进程。
  未启动ROS控制节点或LowCmd，只以生产视觉节点相同的`424×240 Z16 @ 30 FPS`预热30帧后
  抓取一帧，并用现有`prepare_realsense_depth()`/`preprocess_depth()`生成实际`58×87`策略输入。
- 原始有效像素率`92.9%`，策略画面均值约`1.15 m`。画面上下、左右方向正常，地面由画面下方
  向远处延伸，中央箱体竖直，没有180°倒置或明显roll倾斜。与上一次V1-Log首帧并排后，
  地面转换行均约在策略图第18～20行，没有明显俯仰级别偏移；两次场景和机身姿态不同，
  该静态对照不是精确外参标定。
- 本地资产保存在`/home/tang/extreme-runtime-logs/camera-check-20260730-y91sTO/`，包含原始/中间/策略
  深度NPZ、`camera_mount_depth_check.png`和`camera_mount_vs_previous.png`。抓帧后Jetson网络不可达；
  未能确认是整机重启、网络线脱落还是其他断联，也未能确认远程临时目录清理命令是否执行。
- Jetson恢复网络后，只读订阅`/lowstate`50个样本，没有创建LowCmd或启动控制节点。IMU roll
  中位数/范围为`-3.356°/[-3.357°,-3.352°]`，pitch为`-0.305°/[-0.309°,-0.302°]`。
  使用这台D435i已保存的实测内参`fx=fy=213.9745 px, ppx=213.6505, ppy=116.3344`对新原始帧
  的下半部地面做RANSAC平面拟合：相机对地下俯`25.11°`，roll`-2.54°`，光心对地高度
  `0.416 m`，内点率100%，RMSE`1.90 mm`；改变起始行的俯角范围为`25.02°～25.35°`。
  按项目已验证约定扣除机身pitch后，新支架相机相对机身下俯约`25.42°`（合理不确定度
  约`±0.2°`），比训练`20°～25°`上界大约`0.4°`。深度帧和IMU不是同一时刻采集；
  这一结论依赖两次之间机器人和相机未被移动的现场前提。

## 0.45→0.25→0 m分级下箱真机成功（2026-07-30 12:08）

- 用户现场确认本轮完美完成0.45→0.25→0 m分级下箱。Jetson目录为
  `/home/unitree/extreme-runtime-logs/v1log-20260730-120350`，已完整拉回本机同名目录。
  NPZ由用户R2正常停止保存，`reason=r2_emergency_stop`，不是策略保护退出或整机重启。
- 环形记录保存最后250个稳态policy周期、约4.974秒和50帧10 Hz策略深度。循环p50/p95/max为
  `8.27/11.64/16.85 ms`，LowState/深度最大年龄`20.60/55.17 ms`，数值全部有限，`lost`全0，
  最高温度`61°C`。遥控年龄随用户不操作增长到7.65秒，它不是policy运行时停止条件。
- 最初深度边缘在相对`0.333 s`，画面均值从`0.478 m`跃到`1.700 m`。首段过渡姿态幅度仍很大：
  roll最大`+48.85°`、pitch最小`-39.31°`，但约2秒后恢复到roll接近0、pitch约`-14°～-7°`。
  最终一级落地后末帧为roll/pitch `-3.48°/+6.89°`，没有失败轮的延迟pitch发散。
- 前脚重建显示成功轮存在清晰的FR摆动，FR/FL全窗口接触率为`52.4%/100%`；失败0.45 m轮
  为`99.2%/100%`。最后一级下台阶时FR/FL请求最大前伸约`28.9/23.2 cm`，失败轮二次
  失稳段约`16.4/15.6 cm`。成功轮中FL一直承重说明不需要两只前脚同时离地；关键是FR摆动和
  前向落点时机。
- 成功轮输出边界修改`170/250`周期，raw action裁剪`73/250`，都多于失败轮的`121/250`和
  `50/250`。修改原因按周期可重叠统计为步长152、目标机械边界46、预测PD力矩47；最大实际命令步长
  `0.210000 rad`，没有条件式力矩逃逸。这证明“总裁剪周期多”不是失败的充分指标，必须看阶段和
  足端几何。
- 本轮没有实测q/dq运行期越界，但余量不高：FL calf最小q`-2.76312 rad`，距拒绝线
  `-2.7727 rad`仅`0.00958 rad`；RR calf最大速度`19.082 rad/s`，为实际触发线`20.17035`的
  `94.6%`。电机`tau_est`最大比例为RR thigh `118.9%`、RL thigh `109.6%`和FL calf `101.1%`，
  同时静态PD预测被夹在100%内，再次证明冲击期实际估算力矩可高于当前模型。FL/RL末段足力
  峰值为`205/137`，该轮是成功但不是低冲击余量轨迹。
- 新支架与相机在该实验中完成端到端视觉控制；成功画面没有失败轮在下箱后持续占据右侧的
  近距离竖直障碍。结果进一步支持“失败轮右侧物体干扰落脚”为首要候选，但分级路线把单次
  `0.45 m`落差拆成`0.20/0.25 m`，不是只改一个变量的因果A/B。
- 分析图保存为`analysis/depth_contact_sheet.png`和`analysis/front_foot_stride.png`。

## 清空障碍后0.45 m单箱现场成功（2026-07-30）

- 用户在0.45→0.25→0 m分级成功后，使用新支架相机直接进行0.45 m单箱测试，现场确认
  完美成功。该结果与移除先前右侧近距离障碍后恢复成功一致，继续支持视觉干扰候选。
- 已知部署链路是Jetson活动目录`/home/unitree/Extreme-Parkour-Onboard`的V1-Log：对应本地
  `/home/tang/Extreme-Parkour-Onboard` `HEAD a5abebc6e106c49cf41f5783365bf29daf7602de`加未提交的详细运行日志，
  并使用本地`traced/model_38300`成对权重；V3仍在独立目录，不是活动版。从上次完整哈希
  验收后没有再同步过控制或权重文件；本地同期只更新了`MEMORY.md`和分析资产。
- 用户询问刚才运行文件是否就是电脑本地版时，Jetson再次ping/SSH不可达，因此当场无法
  对单箱运行后的远程文件重做哈希核对或拉取最新日志。版本对应结论基于之前的完整远程哈希验收、
  无后续同步变更和刚才V1-Log日志行为；网络恢复后仍应补做这次运行的确切哈希和日志回传。

## 两个0.45 m箱、净间距0.50 m真机失败（2026-07-30 13:39）

- 用户现场测试第一个0.45 m箱后净间隔0.50 m放置第二个0.45 m箱，本轮失败。Jetson目录
  `/home/unitree/extreme-runtime-logs/v1log-20260730-133910`已完整拉回本机同名目录；远近端
  SHA256一致，检查点ZIP完整。日志只留下`reason=periodic_checkpoint`的最后250个控制周期和
  50帧策略深度，没有最终退出记录，因此不能用该文件判断后来是人工停止、进程退出还是整机重启。
- 保存窗口约4.98秒，全程仍处于policy。最终roll/pitch达到`-107.8°/-61.7°`，证明发生真实
  姿态崩溃；电机lost全0，LowState/深度最大年龄约`22/52 ms`，控制循环最大`17.2 ms`，没有
  传感器陈旧或周期超时证据。
- 没有触发运行期q/dq硬限位：最大速度为RR calf `18.503 rad/s`，低于实际触发线
  `20.17035 rad/s`；最接近位置拒绝线的是RR calf `q=-2.770165 rad`，仍有`0.002535 rad`
  余量。分级下箱成功轮的RR calf速度更高（`19.082 rad/s`）、实际力矩峰值比例也更高，故本轮
  不支持通过放宽calf速度、位置或力矩边界来解决。
- 相机持续看到第二个箱体且没有冻结：中央区域深度中位数在约0.70秒内从`0.982 m`减到
  `0.381 m`。稳定接近段FR/FL接触判定均为100%，没有形成两前腿同时离地的直接跨越动作。
  约相对`4.2 s`时，actor请求的FR/FL足端前后位置约为`-9.9/+27.8 cm`，是明显单侧前伸；
  输出保护把FR calf请求从`-1.881`限制为`-1.438 rad`，使FR执行目标进一步后移约8.9 cm，
  但FR落后首先已存在于actor请求，安全层只是次要加剧因素。
- 第二箱接近约`0.38 m`时姿态开始连续发散：roll/pitch从约`-3°/-4°`发展到
  `-24°/-23°`，随后RR再失去接触并快速侧翻。最符合证据的解释是策略在短暂恢复后采用
  踩/爬第二箱的非对称落脚模式，没有完成跨0.50 m间隙所需的协同跃迁，撞箱或落入箱间后
  失去侧向支撑；检查点没有世界坐标，因此不能区分具体碰撞点或精确路线。
- raw action裁剪`148/250`周期、输出边界修改`212/250`周期，但主要集中在窗口前段动态恢复
  和撞击后的失稳段；平稳接近阶段raw action基本未裁。接近第二箱的最后约0.3秒输出步长约束
  连续介入，值得做仿真A/B诊断，但不应将本轮归因于总裁剪次数或直接放开实机硬保护。
- 分析图保存在本轮`analysis/depth_contact_sheet.png`。安全的后续验证应先在仿真训练相同
  双高箱间隙，并在实机使用保护绳/软垫及更短的`0.25～0.30 m`间隙逐级验证；模型侧应重点
  奖励离开第一箱、箱间净空、第二箱前脚落点时机和左右协调，而非单独调大关节限位。
- 用户从现场视频观察到左后腿可能踩入箱间。日志没有RL先变为完全无接触的证据：RL原始足端力
  在姿态开始发散前后仍约`10～16`，直到roll已达约`-79°`才首次低于接触阈值；但其运动学与
  视频观察一致。RL实测足端相对髋的前后位置在`4.038～4.509 s`由约`+19.6 cm`退到
  `-21.3 cm`，向下伸展由约`19.2 cm`增到`29.3 cm`，随后在`4.600 s`达到`36.7 cm`；
  actor在姿态明显发散前已连续请求RL向后、向下。更准确的解释是RL没有及时转移到第二箱，
  留在第一箱后沿或进入箱间并仍接触箱沿/下方，随后左侧下沉；RR在roll已约`-41°`后才失去
  接触，更像侧翻结果而非最初原因。由于没有基座世界坐标，日志不能区分RL踩到箱沿、内壁还是地面。
- 后腿复核图保存为`analysis/rear_leg_failure_trace.png`；其中负roll对应本轮左侧持续下沉，
  结合RL低载下探使“左后脚进入缝里”成为比单纯“正面撞第二箱”更贴合现场视频的失败路径，
  但上游原因仍是策略没有完成跨间隙所需的后腿转移，而不是q/dq硬限位触发。

## 飞行记录窗口扩展到10秒（2026-07-30）

- 默认环形缓冲由250个50 Hz控制周期和50帧10 Hz策略深度，扩展为500个控制周期和100帧
  策略深度，两条数据流均约覆盖最近10秒。NPZ字段和format v4保持不变，旧分析工具可继续读取；
  仅容量和文件大小增加，未压缩深度内存约由1.01 MB增至2.02 MB。
- README已加入当前V1-Log单终端标准带日志启动命令：适用于Jetson活动目录、正装D435i、
  `traced`成对权重、parkour和timer循环；视觉节点后台启动并等待深度话题，控制节点前台运行，
  退出时清理视觉进程。每轮在`~/extreme-runtime-logs/v1log-<时间>/`分开保存视觉、控制和
  10秒飞行记录。命令包含`--nodryrun`，文档明确其真实LowCmd影响及dry-run删除方式。

## 两个0.45 m箱、净间距0.40 m真机成功（2026-07-30 15:27）

- 用户现场确认本轮完整成功跨过两个0.45 m箱之间的0.40 m净间距。Jetson目录为
  `/home/unitree/extreme-runtime-logs/v1log-20260730-152720`，已完整拉回本机同名目录；三个
  文件的远近端SHA256一致。飞行记录由用户R2正常停止保存，`reason=r2_emergency_stop`，不是
  安全保护退出、控制进程异常或整机重启。
- Jetson部署的控制代码、视觉代码、安全层、配置及两份权重与本地一致；只有`flight_recorder.py`
  仍是同步前的5秒版本。因此本轮NPZ只有250个policy控制周期、50帧策略深度和约4.980秒，尚未
  实际使用本地已修改的10秒环形缓冲。
- 第二箱约在保留窗口`1.76 s`进入中央视野，约`2.76～2.86 s`从近处主体视野清出；据深度、姿态、
  足端运动学和足力，核心跨越窗口约为`1.60～3.25 s`。该窗口roll为`-4.51°～+8.23°`，pitch为
  `-10.56°～+3.64°`，FR/FL/RR/RL接触率为`100%/100%/97.56%/100%`，属于持续支撑下的顺序换脚，
  不是四足同时腾空跳跃。
- 后腿转移顺序清晰：RL实测足端前后位置约由`-17.8 cm`前摆至`+23.6 cm`并在约`3.14 s`升载；
  RR随后由约`-29.7 cm`前摆至`+19.2 cm`并在约`3.24 s`升载。相比0.50 m失败轮中RL向后、向下
  探入间隙，本轮关键差异是RL先落上第二箱并建立支撑，然后RR跟进。
- 核心跨越窗口raw action裁剪为`0/82`周期；输出边界修改`52/82`周期，主要是稳态目标步长
  约束（51周期），目标机械边界仅2周期、预测PD力矩边界仅3周期。最大关节速度为RL calf
  `13.505 rad/s`（实际触发线的66.95%），最大实测力矩比例为FL thigh 89.87%，raw action最大
  4.446（低于4.8）。因此本次成功不依赖放宽q/dq硬限位，0.50 m失败也不应通过直接取消保护解决。
- 全5秒窗口包含前一阶段爬箱/恢复和成功后的运动，因而总计raw action裁剪`68/250`、输出修改
  `182/250`不能作为跨越因果指标。全窗口最小位置余量为FL calf距拒绝线`0.01244 rad`；FR thigh
  `tau_est`峰值为额定值112.5%，表明成功轨迹仍存在冲击/负载余量风险，但没有运行期q/dq硬限位。
- 循环p50/p95/max为`8.32/12.94/15.78 ms`，LowState和深度最大年龄为`19.06/56.80 ms`，没有
  控制超时、深度冻结或新增电机lost。分析图保存在成功轮`analysis/depth_contact_sheet.png`、
  `analysis/success_trace.png`和`analysis/rear_transfer_comparison.png`。

## 后续0.40 m复测在首箱触发FL calf状态保护（2026-07-30 15:42）

- Jetson目录`/home/unitree/extreme-runtime-logs/v1log-20260730-154218`已完整拉回本机同名目录，
  controller、visual和两个NPZ的远近端SHA256一致。主记录`reason=policy_guard_recovery`，之后
  由用户R2正常停止；没有控制进程或整机重启证据。主记录包含430个控制周期、约8.60秒，说明
  500周期版本的环形缓冲本轮已实际生效。
- 直接停止原因是policy运行期实测位置保护：FL calf（policy joint 5）达到`-2.778775 rad`，
  越过名义下限`-2.722700 rad`加`0.050 rad`容差后的拒绝线`-2.772700 rad`约`0.006075 rad`。
  策略和输出安全层发送的目标最低均为`-2.700 rad`，没有命令关节越过名义机械下限；这是高速
  收腿产生的实测动态过冲，不是目标位置限位把动作推向越界。
- FL calf在约0.10秒内连续5周期请求目标下界。最后几个policy样本从`q=-1.924 rad, dq=-9.44`
  发展到`q=-2.737 rad, dq=-4.52 rad/s`，中间负速度峰值`-12.425 rad/s`，仅为实际速度触发线
  的61.6%。拒绝后的首个stand recovery样本已为`q=-2.754 rad, dq=+1.93 rad/s`，说明该关节当时
  已越过转折点并回弹；保护捕获的是短暂约6 mrad超线，而非持续卡死或速度失控。
- 视觉证明本轮尚未真正进入两个箱之间的0.40 m跨越：policy开始时中央箱面约`0.99 m`，约2.89秒
  后减到`0.11 m`并充满视野，保护发生在第一个0.45 m箱的上箱/贴近箱面阶段。深度连续、有限且
  没有冻结。故本轮不能作为“0.40 m间隙策略失败”的样本，只能说明首箱爬升阶段被状态保护中断。
- 与15:27成功轮最危险的同类FL calf收腿相比，两轮目标同为`-2.700 rad`；成功轮raw action最低
  反而更大（`-7.070`对`-6.153`），但成功轮负速度峰值为`-10.796 rad/s`，本轮快约15%。成功轮
  最低实测`q=-2.760261 rad`，距拒绝线仅`0.012439 rad`；本轮只比该成功最低点再低约
  `0.018514 rad`。这说明当前拒绝线靠近策略正常上箱动作的动态包络，raw action大小本身不是
  是否触发的决定因素。
- 两轮收腿时的场景与支撑相位不同：本轮初次请求`-2.700 rad`时中央深度约`0.15 m`、roll/pitch
  约`-3.25°/-14.54°`、FR/FL/RR/RL足力为`60/12/8/42`；成功轮同类时刻中央视野约`2.0 m`、
  roll/pitch约`+5.65°/-39.47°`、足力为`10/14/35/54`。本轮在FR/RL对角支撑且RR卸载时快速收FL，
  成功轮则以后腿支撑为主且机身已进入更深的爬箱姿态。最符合证据的上游差异是上箱落脚/支撑
  时序，而不是相机故障、速度硬限位或力矩过载。
- 若只针对本次瞬时误停，状态容差至少需大于`0.056075 rad`，`0.060 rad`可覆盖该样本但只剩
  约3.9 mrad余量；不应直接给所有关节统一放宽。更稳妥的修复方向是为calf下界增加基于q/dq的
  提前制动或仅对calf做经仿真和保护绳验证的微小容差A/B。即使不触发保护，本轮仍贴近箱面且
  支撑相位不同，日志不能证明它随后一定能成功上箱。
- 分析图保存在本轮`analysis/depth_contact_sheet.png`和`analysis/fl_calf_guard_comparison.png`。
