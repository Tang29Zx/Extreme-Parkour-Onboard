# D435i真实深度预处理迁移

## 用户目标

用已在Jetson和真实D435i上验收的深度路径替换仓库原有的`640×480`手工裁剪、
3米归一化和自适应平均池化，同时保持控制节点接收的`/forward_depth_image`
为展平的`58×87`归一化深度。

## 验收标准

- 相机请求`424×240` Z16、30 FPS并读取设备`depth_scale`。
- 原始深度严格按`424×240 -> 60×106 -> 58×87`处理。
- `0/65535/超2米`按2米处理，结果全部有限。
- 相机超时、缺帧、帧号停滞和时间戳停滞均拒绝发布。
- 不旋转、不镜像、不翻转；bicubic后不新增截断。
- `/forward_depth_image`仍包含`58×87=5046`个浮点数。
- 聚焦测试、语法检查和`git diff --check`通过。

## 非目标

- 不修改策略网络内部的关节顺序、模型加载和策略观测数学；只在Unitree消息
  边界执行模型顺序与真机顺序转换。
- 不连接相机或机器人，不运行真实电机命令。

## 回滚

本次修改集中在新增`depth_processing.py`、测试以及`visual_extreme_parkour.py`，
可按聚焦diff逐文件撤销，不影响模型文件。

## Dry-run输出隔离

- 默认dry-run只发布到随机`/lowcmd_dryrun_<id>`话题。
- dry-run不创建`/api/robot_state/request`或`/api/sport/request`发布端。
- dry-run中即使按下遥控器按键，Sport Mode请求也必须在函数边界被拒绝。
- `--nodryrun`保持上游的真实话题和策略数学，但必须经过下述接管保护。

## 真实接管保护

用户目标是保留上游模型、观测、关节顺序和控制增益，只在真实输出边界
增加可验证的安全状态。

验收标准：

- 模型warm-up必须发生在Sport Mode释放前。
- 真实接管要求L1先释放、再连续按住1秒，并要求LowState和遥控器新鲜。
- CheckMode为空时不重复ReleaseMode；ReleaseMode后必须再次CheckMode确认为空。
- L1确认后、ReleaseMode前预创建真实`/lowcmd` publisher和消息缓冲，但在
  CheckMode确认为空前，任何真实LowCmd发布（包括退出时motor-off）都必须被拒绝。
- ReleaseMode前锁存新鲜且近似静止的实测关节角；CheckMode确认为空后立即用该
  位姿发送第一帧保持命令，不等待原生publisher endpoint从DDS图中消失。
- 接管后以锁存位姿为起点，用3秒五次平滑过渡到策略默认站姿。
- 启动站姿和每次L2恢复完成后，必须保持默认站姿至少0.5秒，重建至少10帧
  本体历史和5帧视觉GRU状态；LowState或深度超过0.25秒时重新开始预热。遥控器
  只在Y按下的瞬间检查新鲜度，避免其空闲时不发布导致prime永久无法完成。
- Y只在预热完成后生效。进入策略的前1秒使用五次渐变，每周期目标变化不超过
  0.05 rad；为复现2026-07-28 15:59真机配置，当前稳态12个关节每周期目标变化
  统一不超过0.20 rad，并持续应用机械限位和PD力矩约束。
- `last_actions`必须保持训练语义：保存上一帧actor原始输出。接入渐变和真实输出
  约束只改变LowCmd目标，不改写策略的动作历史。
- policy运行期间每周期检查LowState和深度新鲜度；深度超过0.25秒时停止策略并进入
  站姿恢复，LowState超过0.25秒时发送motor-off尾帧并进入硬停止，禁止继续复用
  失去反馈的状态。
- 如果目标步长、机械限位和PD力矩约束的交集为空，禁止用站姿恢复重发上一条不可行
  目标，必须发送motor-off尾帧并锁定在硬停止状态。
- dry-run使用相同的站姿预热和Y接入状态机，但继续隔离真实LowCmd与Sport API。
- 控制周期不逐帧打印tensor；最近5秒控制和视觉状态保存在内存，L2、R2、异常或
  Ctrl+C时写入NPZ飞行记录。
- 控制节点以不高于2 Hz向`/extreme_parkour/runtime_status`发布版本化JSON状态，供
  独立只读终端实时查看phase、真实输出授权、输入年龄、roll/pitch、足端力、关节
  跟踪、温度、`lost`、估算力矩比例和最近控制循环统计。状态不得包含原始深度图，
  不得在控制循环写磁盘；构建或发布失败只能停止本轮观测，不能影响LowCmd。
- 实时读取工具只能订阅状态话题和`/rosout`，不创建控制、Sport API或robot-state
  发布端；默认只显示`unitree_ros2_real`的ROS日志，避免其他节点刷屏。

风险与约束：MotionSwitcher的CheckMode空状态是低层控制权释放的授权事实；DDS
endpoint的销毁时间不再作为授权条件。首次真机验收仍必须使用承重支架。若ReleaseMode
或其后的CheckMode失败，节点不得发布任何真实LowCmd。

回滚方式：恢复“释放后等待外部publisher清空再创建自身publisher”的顺序；该顺序
会重新引入无低层保持命令的空窗，回滚版本不得用于无支撑真机接管。

Y接入保护的回滚边界：可以移除0.5秒状态预热、1秒渐变、0.05 rad接入限速和
稳态全关节0.20 rad限速以恢复上游
直通，但已观测到第一帧约0.453 rad不对称目标跳变并导致支架侧翻；回滚版本不得再次
真机进入policy。

实时状态的回滚只删除状态publisher、纯状态构建器和只读脚本，不得改动控制环形缓冲、
飞行记录、LowCmd或安全状态机。

## 策略动作裁剪一致性

`normalization.clip_actions`定义缩放后的关节残差上限。策略输出必须先除以
`control.action_scale`换算策略空间裁剪阈值，再乘缩放系数转换为关节残差：

```text
policy_clip = clip_actions / action_scale
clipped_action = clamp(raw_action, -policy_clip, policy_clip)
target_q = default_q + clipped_action * action_scale
```

验收标准：

- `clip_actions=1.2`、`action_scale=0.25`时，策略动作裁剪上限为`±4.8`，任一关节
  残差不得超过`1.2 rad`。
- `self.actions`和下一帧`last_actions`必须保存actor原始输出；训练端在动作延迟和
  裁剪前写入动作历史，因此不能用裁剪后或实际执行动作替代。
- 飞行记录同时保留原始动作、裁剪后请求目标和保护后的实际动作。
- 非有限动作、非法裁剪上限、非法缩放系数或不存在安全可行目标时，必须在LowCmd
  发布前拒绝。

非目标：不修改策略模型、动作维度、PD增益或接入渐变参数。

回滚方式：恢复临时的策略空间`±1.2`裁剪会把关节残差进一步限制为`±0.3 rad`，
但会偏离训练动作分布；该版本只能用于明确接受能力损失的保守诊断。

## model_38300策略边界修复

Isaac内部资产顺序是`FL/FR/RL/RR`，但训练环境在进入actor前使用
`reindex([3,4,5,0,1,2,9,10,11,6,7,8])`，所以`model_38300`的actor输入、
动作和足端接触顺序实际是`FR/FL/RR/RL`，与Unitree协议一致。导出模型内部不包含
额外重排。

验收标准：

- LowState、actor动作和LowCmd统一使用`FR/FL/RR/RL`，关节映射为identity。
- Unitree脚端力保持`FR/FL/RR/RL`，足端映射为identity。
- hip/thigh/calf内部顺序和全`+1`方向保持不变。
- 测试必须从训练源码的actor契约出发，用一热动作证明actor第0维进入FR电机0；
  只验证自定义映射的往返或对称站姿不构成验收。
- 修复只做离线测试；重新同步Jetson并通过dry-run前禁止真实接管。

回滚方式：恢复错误的二次重排会重新交换前后腿的左右动作，回滚版本不得进入真机
policy。

## Y后向左侧翻根因修复

2026-07-27飞行记录证明，Y后的第一帧和接入期0.05 rad限速正常，模型输出、深度和
控制周期没有非有限值。但该记录的关节和足端字段按错误的`FL/FR/RL/RR`假设标记，
不能排除映射；训练源码复核已经证明二次重排会交换左右动作与动态接触。旧阈值25
同时造成支撑误判，因此映射与接触阈值都必须修复，不能只改阈值。

验收标准：

- 真机脚端力使用实测区分支撑与离地的阈值5，并使用训练一致的一帧接触记忆。
- policy prime期间每帧要求四足承重、roll/pitch不超过8°、最大关节跟踪误差不超过
  0.2 rad且12个`lost`计数不增长；prime开始时锁存现有计数，历史非零值允许通过。
  prime完成后的`stand_hold`继续逐周期观察计数，增长时立即更新基线并自动重新执行
  完整0.5秒prime，不能把等待期间已经稳定下来的历史增长延迟到下一次Y才拒绝。
  Y瞬间仍重复检查，Y当帧的新增长必须拒绝。温度只记录，不阻断Y。
- 接入前1秒除0.05 rad/周期限制外，相对接入起点的单关节偏移不超过0.3 rad；
  之后12个关节统一以0.20 rad/周期限制，并持续执行机械限位和PD力矩约束。
- 运行期关节速度检查允许URDF标称上限的0.5%相对测量容差，避免传感器量化和PhysX
  浮点误差在边界值制造误停；超过容差仍必须离开policy。
- 接入后的姿态和支撑变化只记录，不触发自动motor-off；电机关闭仍由R2或进程退出
  明确触发，LowState运行期超时除外。L2始终沿用一秒站姿渐变，不增加倾斜角分支。
- NPZ格式升级为2，增加四足接触判定以及12电机温度、`lost`和估算力矩。

非目标：不修改模型、固定0.5 m/s命令、深度链路或PD增益，
也不增加基于roll/pitch或足端接触的运行期自动急停。
真实阈值5来自本次日志中承重脚最低8～11、明确离地脚为0的观测，仍需在Jetson
dry-run中检查不同地面和支架载荷下的裕量。

回滚方式：逐文件撤销本节对应的接触滤波、入口检查、接入包络和NPZ v2字段。
旧阈值25已经在真实记录中制造左右接触误判，回滚版本不得再次进入真机policy。
只回滚`stand_hold`持续`lost`监测会恢复“长时间等待后第一次Y因旧增长被拒绝”的
可用性问题；虽然仍会安全拒绝，但不得描述为已修复。

## Unitree LowState/LowCmd边界S2S

用户目标是在不连接ROS、DDS或真机的情况下，让Isaac Gym闭环真实经过部署端的
Unitree消息边界，直接观察正确映射、生产安全链和历史错误映射对运动结果的影响。

验收标准：

- PhysX原始`FL/FR/RL/RR`状态必须先编码成Unitree `FR/FL/RR/RL` LowState等价
  数据，再由生产端共用的解析和观测构建函数进入actor。
- actor输出必须依次经过`prepare_policy_action()`、`PolicyTransitionGuard`和
  `constrain_policy_target()`，编码成LowCmd等价数据后再转回Isaac原始顺序。
- Isaac Viewer默认显示三条相同赛道：蓝色直连基准、绿色修复后的完整Unitree边界、
  红色仅在仿真存在的旧二次重排故障对照。三路使用相同的相对初始状态、固定
  `0.5 m/s`命令、深度射线和20 cm单箱。
- 三路由共享的遥控器上升沿逻辑注入L1，随后保持3秒站姿并执行0.5秒policy prime；
  prime完成后的独立Y上升沿才允许策略接入。回放记录LowCmd授权状态，但不模拟
  MotionSwitcher、DDS或真实publisher所有权。
- 单箱地形支持固定种子的块状高度噪声；验收运行使用±5 mm幅度和0.2 m噪声块，
  NPZ必须记录噪声参数、L1/Y周期及每帧遥控器状态。
- 绿色边界回放以1000步为上限，必须越过箱体后缘；到达最后赛道目标属于成功终止，
  roll/pitch/height终止或其他reset属于失败。接入期每帧目标变化不超过0.05 rad；
  稳态12个关节均不超过0.20 rad，目标不越机械限位，估算PD
  力矩不超过Go2 URDF限值。
- 日志必须同时保存actor、Unitree motor和Isaac三种顺序，且使用无pickle的NPZ。

非目标：不验证DDS序列化、CRC正确性、Unitree固件、电机通信、真实相机时序或真实
脚端力尺度；红色故障对照不要求必然侧翻。

风险与回滚：生产节点只能改为调用消息无关纯函数，ROS消息、CRC和publisher行为
保持原位。边界回放由独立脚本提供，可单独撤销；旧映射常量不得进入生产模块。

## 飞行记录保存策略深度输入

用户目标是在真机动作异常后直接查看actor视觉分支实际使用的深度，而不是仅依赖
`min/max/mean`和34维视觉输出推测画面内容。

验收标准：

- 每个视觉记录必须保存与本次视觉编码严格相同的归一化`58×87 float32`深度输入；
  不得记录刚收到但要到下一次视觉更新才会使用的新帧。
- 深度图与既有`visual_timestamp`、`depth_stats`和`visual_output`保持同一条记录、
  相同容量和相同顺序。
- 默认50帧环形缓冲约覆盖5秒；新增未压缩内存约1.01 MB，不改变50 Hz控制记录容量。
- 控制和视觉回调只复制到内存；磁盘写入仍只能发生在既有flush路径，并继续使用临时
  文件加原子重命名。
- NPZ升级为`format_version=3`并新增`depth_input`，固定形状为`(N,58,87)`；没有
  视觉样本时固定为`(0,58,87)`，旧分析工具可继续读取原字段。
- 非有限值或错误形状必须拒绝该视觉样本，不能写出形状不稳定或需要pickle的记录。
- 单元测试必须覆盖环形淘汰、数值原样保存、空视觉形状和非法输入拒绝；Python 3.8
  语法检查及`git diff --check`必须通过。

非目标：本轮不保存`424×240 uint16`相机原始帧，不修改ROS话题、视觉网络、深度
预处理、策略动作或安全状态机，也不连接D435i或真机。

风险与回滚：压缩NPZ会在退出时增加约1 MB输入数据的压缩开销，但不会进入控制循环；
如Jetson退出时延迟不可接受，可只撤销`depth_input`字段和格式版本，既有v2字段保持
不变。
